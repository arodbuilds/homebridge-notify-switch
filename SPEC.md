# homebridge-notify-switch v1 Specification

Status: Current as of 1.2.0
Repository: https://github.com/arodbuilds/homebridge-notify-switch
Package: homebridge-notify-switch
Author: arodbuilds

## 1. Purpose

A Homebridge dynamic platform plugin that exposes HomeKit switches. Each switch is always off. Turning it on sends one or more preset messages through configured providers and the switch returns to off on its own. Switches are meant to be triggered by HomeKit automations and scenes, giving any HomeKit event a way to notify people by SMS, email, Telegram, or ntfy push notification.

## 2. Goals and non-goals

### Goals

1. One plugin, multiple channels, one configuration model.
2. Minimum viable setup is a single provider, a single group, a single switch, configurable entirely from the Homebridge UI with no config.json editing.
3. Meets every current Homebridge Verified requirement.
4. Runs comfortably on a Raspberry Pi Zero.
5. Credentials are handled with least privilege and never logged.
6. Configuration shape supports future channels without migration.

### Non-goals for v1

1. Inbound messages or replies.
2. Delivery status callbacks or webhooks.
3. MMS or attachments.
4. WhatsApp (planned for a later version).
5. Fallback routing (send on channel B only if channel A fails). v1 sends on every configured channel in parallel.

Planned for 1.1: WhatsApp via Twilio, using a single approved utility template with the message body as the template variable, so no per-message template approval is needed. Voice calls via Twilio are a candidate for the same release.

## 3. Terminology

1. Provider: a configured connection to an external messaging service. Types: `twilio`, `smtp`, `telegram`, and, from 1.1.0, `ntfy`.
2. Channel: the kind of message a provider can send. Channels: `sms`, `email`, `telegram`, `ntfy`. The Twilio provider can serve both `sms` and `email`; the ntfy provider serves `ntfy`.
3. Recipient group: a named list of people, holding an address list per channel.
4. Switch: a HomeKit switch accessory with one or more actions.
5. Action: one send on one provider, with its own message body, targeting one or more groups and optionally extra individual recipients. Actions are the stored form in config.json; from 1.1.0 the custom UI does not show them and derives them from the switch editor (section 11.2, item 8).
6. Platform default: the provider switches use on a channel when more than one provider serves it and the action does not name one (section 5.7).

## 4. Platform and accessory model

1. Plugin type: dynamic platform. `pluginAlias` is `NotifySwitch`. `singular` is true (one platform block).
2. Each configured switch becomes one accessory with a `Switch` service.
3. Each switch has a stable `id` (UUID v4 generated by the UI when created; also accepted if typed). The accessory UUID is derived from `id`, never from the display name, so renames do not orphan the accessory in HomeKit.
4. On startup the platform registers accessories for every valid switch and unregisters any cached accessory whose `id` is no longer in config.
5. Optional per switch: a `ContactSensor` service that trips on send failure (see section 9). Default off.
6. Optional platform-level master switch named by the user (default "Notifications Enabled"). When off, no switch sends anything; flips are logged as suppressed. Default enabled and shown.
7. The plugin does not register any accessory if the platform config is missing or fails validation. It logs the reasons and exits its startup path cleanly. It never throws. Cached accessories are left untouched in that case, so HomeKit automations survive a typo.
8. Child bridge is supported and recommended in the README.
9. A configuration that passes validation with zero switches (a fresh install, or after Reset plugin to fresh install in the settings UI) registers nothing and unregisters every cached accessory, the master switch included. The state persisted in their contexts (the master switch position and the failure sensor state) is cleared with them.

## 5. Configuration schema

All configuration lives in the platform block. Field names are final unless changed during review.

```json
{
  "platform": "NotifySwitch",
  "name": "Notify Switch",
  "configVersion": 1,
  "defaultCountry": "US",
  "masterSwitch": { "enabled": true, "name": "Notifications Enabled" },
  "debug": false,
  "defaultProviders": { "email": "fastmail" },
  "providers": [
    {
      "id": "twilio-main",
      "type": "twilio",
      "name": "Twilio",
      "apiKeySid": "SK...",
      "apiKeySecret": "...",
      "accountSid": "AC...",
      "smsSenders": ["+16785550100"],
      "emailFrom": { "address": "alerts@example.com", "name": "Home" }
    },
    {
      "id": "fastmail",
      "type": "smtp",
      "name": "Fastmail",
      "host": "smtp.fastmail.com",
      "port": 465,
      "security": "ssl",
      "username": "alex@example.com",
      "password": "app-password",
      "from": { "address": "alex@example.com", "name": "Home" }
    },
    {
      "id": "telegram-home",
      "type": "telegram",
      "name": "Telegram",
      "botToken": "123456:ABC..."
    },
    {
      "id": "ntfy",
      "type": "ntfy",
      "name": "ntfy",
      "server": "https://ntfy.sh",
      "auth": "token",
      "token": "tk_..."
    }
  ],
  "groups": [
    {
      "id": "family",
      "name": "Family",
      "sms": ["+16785550101", "+16785550102"],
      "email": ["a@example.com"],
      "telegram": ["123456789", "-1001234567890"],
      "ntfy": ["home-alerts-x7q2"]
    }
  ],
  "switches": [
    {
      "id": "6f1c2a9e-2b1c-4b8f-9d1e-0c5a1e2f3a4b",
      "name": "Water Leak Alert",
      "enabled": true,
      "cooldownSeconds": 60,
      "failureMode": "any",
      "failureSensor": false,
      "actions": [
        {
          "providerId": "twilio-main",
          "channel": "sms",
          "sender": "+16785550100",
          "groups": ["family"],
          "recipients": [],
          "body": "Water detected under the kitchen sink at {{time}}."
        },
        {
          "providerId": "fastmail",
          "channel": "email",
          "groups": ["family"],
          "recipients": [],
          "subject": "Water leak: kitchen",
          "body": "Water detected under the kitchen sink at {{time}} on {{date}}."
        },
        {
          "providerId": "ntfy",
          "channel": "ntfy",
          "groups": ["family"],
          "recipients": [],
          "subject": "Water leak: kitchen",
          "priority": "high",
          "tags": ["warning", "house"],
          "body": "Water detected under the kitchen sink at {{time}}."
        }
      ]
    }
  ]
}
```

### 5.1 Platform fields

1. `name` (string, required). Platform display name. Homebridge prefixes every log line with it, so it follows the display name rule of section 5.2 (a startup warning, never an error).
2. `configVersion` (integer, hidden, default 1). Used for future migrations.
3. `defaultCountry` (ISO 3166-1 alpha-2, default `US`). Used by the UI phone number entry and by startup normalization of numbers lacking a plus sign. The custom UI prefills it on first load from the browser locale, else from the Homebridge host's time zone, else `US` (section 11.2, item 21); startup normalization always uses the saved value, `US` when absent.
4. `masterSwitch.enabled` (boolean, default true). Whether to expose the master switch accessory.
5. `masterSwitch.name` (string, default "Notifications Enabled").
6. `debug` (boolean, default false). Verbose logging; never includes secrets even when on.
7. `defaultProviders` (object, optional, from 1.1.0). Maps a channel (`sms`, `email`, `telegram`, `ntfy`) to the id of the provider switches use on that channel unless an action names another. Only channels with more than one provider carry an entry (section 5.7).
8. `timeFormat` (enum `12h`, `24h`; default `12h`, from 1.2.0). How `{{time}}` and the time part of `{{datetime}}` render (section 5.6).
9. `dateFormat` (enum `mdy`, `dmy`, `ymd`; default `mdy`, from 1.2.0). How `{{date}}` and the date part of `{{datetime}}` render (section 5.6).

Both are closed enums. A missing field means the default. An invalid value is handled exactly as an invalid `defaultCountry`: a startup error at `platform.timeFormat` or `platform.dateFormat` (`must be one of 12h, 24h`), with the default used for the rest of the validation pass. The custom UI shows both under Settings, directly below Default Country (section 11.2, item 27), and writes them with the next Save like Default Country; a stored value it does not recognise is shown and written as the default. Reset plugin to fresh install keeps the defaults. The schema form carries both as enums with the same titles (`Time format`, `Date format`) and one-line descriptions.

### 5.2 Provider fields

Common to all providers: `id` (slug, unique, required), `type` (enum, required), `name` (display name, required).

Display name rule (providers and groups, from 1.1.0): printable characters only, no control characters (C0, C1, DEL, the line and paragraph separators), no angle brackets, 1 to 64 characters; pattern `^[^\u0000-\u001F\u007F-\u009F\u2028\u2029<>]{1,64}$`, in the spirit of the switch name rule. `config.schema.json` and the custom UI enforce it (validation message in section 11.3: "Use letters, numbers, spaces, and punctuation, up to 64 characters."); startup validation reports a name that breaks it as a warning naming the field (`providers[0].name`, `groups[0].name`, `platform.name`), never an error, so an existing configuration keeps starting.

Twilio (`type: twilio`). Serves channels `sms` and `email`.

1. `accountSid` (required, pattern `^AC[0-9a-fA-F]{32}$`).
2. `apiKeySid` (required, pattern `^SK[0-9a-fA-F]{32}$`).
3. `apiKeySecret` (required, password field).
4. `smsSenders` (array of E.164 strings, `uniqueItems`). At least one of `smsSenders` or `messagingServiceSid` is required to use the `sms` channel.
5. `messagingServiceSid` (optional, pattern `^MG[0-9a-fA-F]{32}$`). When present it must match; an empty string is rejected by the schema, so the custom UI omits the key when the field is blank.
6. `emailFrom.address` and `emailFrom.name` (optional). Required to use the `email` channel. The address domain must be authenticated in the Twilio Console under Communications > Email > Domains. The field stays optional in the schema; an email action on a Twilio provider without `emailFrom.address` is a startup validation error (`switches[i].actions[j].channel: provider "x" cannot send email until emailFrom.address is set on it`), which the custom UI shows on the switch's Email provider dropdown rather than prompting for the address (a Twilio provider without it does not count as an email provider for the platform defaults of section 5.7).

Auth Token is deliberately not accepted. The README and field help explain that an API key can be revoked without rotating the account's master credential.

SMTP (`type: smtp`). Serves channel `email`.

1. `host` (required).
2. `port` (integer, required, default 465).
3. `security` (enum `ssl`, `starttls`, `none`; default `ssl`). Maps to nodemailer `secure: true`, `requireTLS: true`, and neither, respectively. With `none` nodemailer still upgrades opportunistically with STARTTLS when the server offers it, with certificate verification on.
4. `username` (required).
5. `password` (required, password field).
6. `from.address` (required, email format) and `from.name` (optional).
7. `smtpPreset` (optional, string). The mail provider preset the custom UI last chose (`fastmail`, `gmail`, `icloud`, `outlook`, `yahoo`, `zoho`), kept for redisplay only (section 11.2, item 22). The runtime never reads it; `host`, `port` and `security` are the settings that count. `config.schema.json` declares it as an optional string with that enum, hidden from the schema form (which has no picker), so a stored value passes the schema.

No option to disable certificate verification is offered.

Telegram (`type: telegram`). Serves channel `telegram`.

1. `botToken` (required, password field, pattern `^\d+:[A-Za-z0-9_-]{30,}$`).
2. `parseMode` (enum `none`, `markdown`, `html`; default `none`). `markdown` sends the Bot API's legacy `Markdown` mode rather than `MarkdownV2`, which requires escaping most punctuation and would make plain alert text fail with 400; `html` sends `HTML`; `none` omits `parse_mode`.

ntfy (`type: ntfy`, from 1.1.0). Serves channel `ntfy`.

1. `server` (URL, default `https://ntfy.sh`). Must be `http` or `https`, without credentials, query or fragment; a path is allowed for a server behind a prefix; trailing slashes are dropped. The URL parser's normalized form is what the plugin uses and logs.
2. `auth` (enum `none`, `token`, `basic`; default `none`).
3. `token` (password field, required when `auth` is `token`; no whitespace or control characters, up to 256 characters). Sent as `Authorization: Bearer {token}`.
4. `username` and `password` (required when `auth` is `basic`; the username holds no colon). Sent as `Authorization: Basic base64(username:password)`.
5. With `auth` `none`, a `token`, `username` or `password` that is present is ignored with a warning.
6. Optional `credentialsFile` as for the other providers (section 12, item 2): its keys may be `token`, `username` and `password`.

### 5.3 Group fields

1. `id` (slug, unique, required).
2. `name` (display name, required).
3. `sms` (array of E.164 strings, optional, `uniqueItems`).
4. `email` (array of email addresses, optional, `uniqueItems`).
5. `telegram` (array of chat IDs matching `^-?\d+$`, optional, `uniqueItems`).
6. `ntfy` (array of topic names matching `^[A-Za-z0-9_-]{1,64}$`, optional, `uniqueItems`). A topic name is what lets anyone read (and publish to) the topic, so it is masked in info logs like a chat id (`ho***ts`).

A group with no addresses is valid but produces a startup warning if any switch references it. Each of the four lists holds at most 200 entries (section 12, item 12). The display name rule of section 5.2 applies to `name`.

### 5.4 Switch fields

1. `id` (UUID, required, generated by the UI).
2. `name` (required). Must satisfy HAP naming: pattern `^[A-Za-z0-9][A-Za-z0-9 ']*[A-Za-z0-9]$`, max 64 characters. Unique across switches.
3. `enabled` (boolean, default true). Disabled switches are still registered but do nothing when flipped, and log the suppression.
4. `cooldownSeconds` (integer, 0 to 86400, default 0). Flips within the cooldown window are logged and ignored.
5. `failureMode` (enum `any`, `all`, `off`; default `any`). Governs when the failure sensor trips and how the result is logged. `any`: any recipient on any channel failing counts as failure. `all`: only when every recipient on every channel fails. `off`: never trips the sensor; failures are still logged.
6. `failureSensor` (boolean, default false). Adds a Contact Sensor service to this accessory. Contact opens on failure and closes on the next successful send or after `failureSensorResetSeconds`.
7. `failureSensorResetSeconds` (integer, default 300). 0 keeps the sensor open until the next successful send.
8. `actions` (array, `minItems` 1). Actions remain the stored form of what a switch sends. From 1.1.0 the custom UI does not edit them directly: it shows the switch editor of section 11.2, item 8 (recipients, channels, one message) and derives the array from it, one action per enabled channel, and reads a stored array back into that form losslessly. A stored switch with more than one action on the same channel cannot be read into that form; the custom UI shows such a configuration not at all (section 11.2, item 26), while startup runs it as before. The schema form and hand edits keep writing actions as before.

### 5.5 Action fields

1. `providerId` (required). Must reference an existing provider.
2. `channel` (enum `sms`, `email`, `telegram`, `ntfy`, required). Must be a channel the referenced provider serves.
3. `sender` (optional, sms only; when present it must be E.164, an empty string is rejected by the schema and the custom UI omits a blank sender). One of the provider's `smsSenders`. If omitted and the provider has exactly one sender, that sender is used (also when `messagingServiceSid` is set: the rules apply in this order). If omitted otherwise and `messagingServiceSid` is set, the Messaging Service is used.
4. `groups` (array of group ids, optional).
5. `recipients` (array of addresses for this channel, optional).
6. At least one of `groups` or `recipients` must resolve to one or more addresses, and at most 100 after deduplication (section 12, item 12).
7. `subject` (email and ntfy, optional, default is the switch name). The email subject, or the ntfy notification title. CR and LF are stripped; the ntfy provider strips every control character from the title and RFC 2047 encodes it when it holds anything outside printable ASCII. Set on another channel it is ignored with a startup warning.
8. `body` (required). Constraints by channel:
   sms: 1 to 160 characters, GSM-7 character set enforced by validation at startup and by the custom UI. `config.schema.json` cannot carry the pattern because `body` is one field shared by every channel, so the schema form's description combines the sms and email/telegram help. The custom UI shows a live character and segment counter.
   email: 1 to 10000 characters. Sent as plain text; HTML is not supported in v1.
   telegram: 1 to 4096 characters. Sent with the provider's `parseMode`.
   ntfy: 1 to 4096 characters, plain text. ntfy.sh limits a message to 4096 bytes, so a body near the limit with many non-ASCII characters is refused by the server with 413.
9. `bcc` (email only, boolean, default false). "Hide recipients from each other (BCC)": when true and the action resolves to more than one recipient, the recipients go in Bcc and the from address in To (sections 6.2 and 6.3). A message to a single recipient always uses To. Set on another channel it is ignored with a startup warning.
10. `priority` (ntfy only, enum `min`, `low`, `default`, `high`, `urgent`; default `default`). Sent as the `Priority` header in its word form. Set on another channel it is ignored with a startup warning.
11. `tags` (ntfy only, array of strings each matching `^[A-Za-z0-9_+-]{1,32}$`, at most 8, duplicates dropped). Sent comma-joined in the `Tags` header; omitted when empty. Set on another channel it is ignored with a startup warning.
12. A switch holds at most 20 actions (section 12, item 12).

### 5.6 Template variables

Available in `body` and `subject` (the email subject or the ntfy title): `{{switchName}}`, `{{time}}`, `{{date}}` and `{{datetime}}`. Unknown variables are left as typed. The time zone is the Homebridge host's; the formats follow the platform's `timeFormat` and `dateFormat` (section 5.1), rendered by `buildTemplateVariables` in `src/template.ts`, which takes the two settings:

1. `{{time}}`: with `12h`, the hour without a leading zero, a colon, two-digit minutes, one space and uppercase AM or PM ("5:15 PM"; midnight is "12:00 AM", noon is "12:00 PM"). With `24h`, two-digit hour and minutes ("17:15"). Never seconds.
2. `{{date}}`: with `mdy`, "9/8/2026"; with `dmy`, "8/9/2026"; with `ymd`, "2026-09-08". Only `ymd` zero-pads the month and day.
3. `{{datetime}}`: the date, one space, the time, in the chosen formats ("9/8/2026 5:15 PM"). No seconds and no zone.

Before 1.2.0 the three rendered as `HH:mm`, `YYYY-MM-DD` and `YYYY-MM-DDTHH:mm:ss`; the old time and date forms are what `24h` and `ymd` give, and `{{datetime}}` no longer carries seconds under any setting.

### 5.7 Platform defaults

From 1.1.0 the platform block carries `defaultProviders`, an object mapping a channel to a provider id. It answers one question: which provider does a switch use on a channel when its action does not say? The runtime never reads it (every stored action carries its `providerId`); the custom UI derives `providerId` from it, and startup validation checks it. The rules, shared by startup validation and the UI (`src/defaults.ts`):

1. A provider serves the channels of its type (section 3, item 2). A Twilio provider serves `email` only once `emailFrom.address` is set.
2. When exactly one provider serves a channel, it is the default. Nothing is shown for it in the UI and nothing is stored; an entry that names it is accepted and ignored.
3. When more than one provider serves a channel, `defaultProviders[channel]` names the default. The UI stores an entry only for such channels and only when it names one of them.
4. A missing entry for a channel with more than one provider falls back to the first of them in configuration order. Startup logs a warning naming the fallback (`platform.defaultProviders.email: 2 providers can send email and none is the default; switches that do not name one use "twilio-main", the first in config order. Choose a default under Settings`). From 1.2.0 this rule is reached only by hand-edited configurations: the custom UI writes the entry the moment it applies (section 11.2, item 25), so a configuration it saved always carries one, and it shows no warning for a missing entry.
5. An entry naming an id that does not exist, a provider whose type does not serve the channel, a Twilio provider without `emailFrom.address` for `email`, a key that is not a channel, or a value that is not a string, is a startup error at `platform.defaultProviders.{channel}` (section 10). The UI replaces such an entry with the current fallback the next time it validates the block, and asks again.
6. Removing the default provider in the UI: if one provider remains for the channel it becomes the default and nothing is stored; if more remain, the UI writes the new fallback (the first remaining validated provider in configuration order) immediately and shows the prompt on the last remaining validated card. Renaming a provider's id under Advanced carries an entry that named it along.
7. In the switch editor a channel's provider is the override the switch stores under Advanced, else the platform default. A stored action whose `providerId` is the channel's resolved default loads as no override, so changing the default later moves that switch with it; one whose `providerId` differs loads as an override and stays put. One whose channel no provider serves any more (the provider it names was removed, or the only provider of its type stopped serving the channel) is kept: it loads with its `providerId` as the override, its channel is listed under Send by disabled with the note "No provider configured for {channel}; add one or untick to remove." (section 11.2, item 8), and it is written back exactly as stored. It is dropped only when the user unticks the channel; adding a provider for the channel makes it an ordinary action again.

## 6. Provider interface

```ts
interface SendRequest {
  channel: Channel;
  sender?: string;
  recipients: string[];
  subject?: string;   // email subject or ntfy title
  body: string;
  bcc?: boolean;      // email only
  priority?: NtfyPriority; // ntfy only
  tags?: string[];    // ntfy only
}

interface RecipientResult {
  recipient: string;
  ok: boolean;
  id?: string;        // provider message id, operation id, etc.
  error?: string;     // sanitized, no secrets, no request dumps
}

interface Provider {
  readonly id: string;
  readonly type: ProviderType;
  readonly channels: Channel[];
  validateConfig(): ValidationIssue[];
  validateBody(channel: Channel, body: string): ValidationIssue[];
  send(req: SendRequest): Promise<RecipientResult[]>;
}
```

Rules:

1. The switch code imports only this interface. Provider modules are loaded lazily by type so an unused provider costs nothing.
2. Every provider request has a 10 second timeout and at most one retry with a 2 second backoff, except when the provider returns a rate limit response with a retry-after value, in which case that value is honored once. SMTP has no retry-after: temporary replies (421, 450, 451, 452) and connection errors are retried once after the standard backoff.
3. Per-provider in-flight concurrency is capped at 5. A provider's HTTP response body is read up to 1 MiB; the rest is discarded and the stream cancelled (section 12, item 12).
4. Providers never throw. All errors resolve to `RecipientResult` entries. One recipient's failure never stops the others: a provider that sends one request per recipient collects the outcomes with `allSettled` semantics (never `Promise.all`), so a 4xx, a thrown error, or a timeout for one recipient changes that recipient's entry only, and every other recipient is still sent and reported.
5. The diagnostics the settings UI server uses (Test connection, Find people and groups, Look up numbers) live on a separate `ProviderDiagnostics` interface; the `Provider` interface above is all the switch code depends on. The render-and-send step is shared between the switch accessory and the settings UI's Test send (`src/send.ts`).

### 6.1 Twilio SMS

POST `https://api.twilio.com/2010-04-01/Accounts/{accountSid}/Messages.json` with Basic auth `apiKeySid:apiKeySecret`, form body `To`, `Body`, and either `From` or `MessagingServiceSid`. One request per recipient, concurrency 5. Success is HTTP 201; the `sid` is returned as `id`. Twilio error `code` and `message` are logged.

### 6.2 Twilio Email

POST `https://comms.twilio.com/v1/Emails` with the same Basic auth, JSON body with `from`, `to` (all recipients in one request, so they see each other), and `content.subject`, `content.text` and `content.html`. The API requires `html`, so the plain body is sent both ways: `text` is the rendered body and `html` is the same body HTML-escaped inside a `pre` element so line breaks survive; no HTML authoring is exposed. With the action's `bcc` option and more than one recipient, the recipients go in `bcc` (the same `{ address }` objects as `to`) and the from address in `to`; a single recipient is always in `to`. Success is HTTP 202; `operationId` is returned as `id` for every recipient. One request per action regardless of recipient count.

From 1.1.1 `content.headers` carries `Auto-Submitted: auto-generated` (RFC 3834, the value for automated messages that are not replies). The endpoint nests custom headers inside `content`, and `Auto-Submitted` is not on its list of headers that may not be overridden. It is the only custom header the plugin sends. Twilio Email adds an open-tracking pixel to every message, and as of September 2026 there is no setting to turn it off, per message or per account: the endpoint documents no tracking field (the SendGrid v3 `tracking_settings` field is not part of it) and the Email settings in the Twilio Console (https://www.twilio.com/docs/email/settings) cover only event forwarding, IP addresses, and the address allow list. So the plugin sends no tracking field and makes no claim about tracking. Twilio Email also sends from shared SendGrid IP addresses whose reputation the household does not control. The README states both and points a household that wants neither at an SMTP provider (section 15, item 3).

### 6.3 SMTP

nodemailer transport built from provider config. One message per action with all recipients in `to` by default. With the action's `bcc` option and more than one recipient, the recipients go in `bcc` and the `from` address in `to` (so the envelope is valid and recipients do not see each other); a single recipient is always in `to`. Success is a resolved `sendMail`; the `messageId` is returned as `id`. Rejected addresses reported by the server are mapped to per-recipient failures (a fully rejected envelope keeps the per-recipient errors). The login is scrubbed from every error string. nodemailer `debug` and `logger` options are never enabled and no `tls` override is passed. From 1.1.1 every message carries the header `Auto-Submitted: auto-generated` (RFC 3834) through nodemailer's `headers` option; it is the only header the plugin adds.

### 6.4 Telegram

POST `https://api.telegram.org/bot{token}/sendMessage` with JSON `chat_id`, `text`, and `parse_mode` when not `none`. One request per chat ID, concurrency 5. Success is `ok: true`; `result.message_id` is returned as `id`. 403 (bot blocked) and 400 (bad chat id) are mapped to readable errors. 429 honors `parameters.retry_after`. The bot token is part of the URL, so it is redacted from every error string.

### 6.5 ntfy

POST `{server}/{topic}` with the rendered body as plain text (`Content-Type: text/plain; charset=utf-8`) and the headers `Title` (the action's `subject`, else the switch name; control characters removed, cut to 200 characters, RFC 2047 encoded outside printable ASCII, omitted when empty), `Priority` (the word form: `min`, `low`, `default`, `high`, `urgent`), `Tags` (comma-joined, only when there are tags) and `Authorization` (`Bearer {token}` or `Basic base64(username:password)`, only when `auth` is not `none`). One request per topic, concurrency 5, the shared 10 s timeout and single retry; 429 honors the `Retry-After` header. Success is HTTP 200 with a JSON body carrying an `id`, returned as `id` per topic; a 200 without one is a failure ("ntfy answered HTTP 200 without a message id; check that the server URL points at an ntfy server"), because a web page at a wrong URL is not a delivery. 401 and 403 map to "ntfy rejected the credentials for topic {topic} (HTTP {status})", 404 to "ntfy server not found (HTTP 404); check the server URL", 413 to "ntfy error 413: the message is too large for this server", 429 to "ntfy error 429: rate limited"; other responses are reduced to ntfy's `code` and `error` fields. The token, the password and the encoded Authorization value are redacted from every error string and never logged.

Test connection (section 11.2, item 3): GET `{server}/v1/health` without credentials; the answer must be HTTP 200 with `healthy: true` (404 reads "ntfy server not found at {server} (HTTP 404). Check the server URL.", anything else "{server} did not answer like an ntfy server (HTTP {status})." or "reports that it is not healthy"). With `auth` set, then GET `{server}/v1/account` with the Authorization header: 200 is "Connected to {server} and the credentials were accepted.", 401 or 403 "ntfy rejected the credentials (HTTP {status}). Check the access token." (or "the username and password"). With `auth` `none` the success line is "Connected to {server}. No credentials are set, so the topics must allow anonymous publishing." Neither request publishes anything; Test send publishes for real.

## 7. Runtime behavior on flip

1. HomeKit sets `On` to true. The handler returns immediately.
2. If the master switch is off, or the switch is disabled, or the cooldown is active, log at info and go to step 8.
3. Resolve recipients for each action: union of referenced groups' lists for that channel plus `recipients`, deduplicated.
4. Render template variables.
5. Fire all actions in parallel. Each provider applies its own concurrency cap.
6. Collect results. Log one line per recipient (see section 8) and one summary line per switch.
7. Evaluate `failureMode` and update the failure sensor if enabled.
8. After 1 second, set `On` back to false and notify HomeKit.

Setting `On` to false from HomeKit is a no-op.

## 8. Logging

1. Info on each send: `[Water Leak Alert] sms via twilio-main to +1678***0101: sent (SM...)`. Phone numbers, email addresses, chat ids and topic names are partially masked in info logs; full addresses appear only when `debug` is true.
2. Warn on each failure with the provider's error code and a short message.
3. Error only for configuration problems at startup.
4. Message bodies are never logged at info. With `debug` true, bodies are logged.
5. Secrets are never logged at any level. Error objects from providers are reduced to code and message before logging.
6. Every line written through the plugin's logger has its control characters escaped (`\n`, `\r`, `\t`, `\u001b`), and every configuration value quoted in a validation message is escaped and cut to 80 characters, so nothing from config.json or a provider's reply can start a new log line or emit a terminal escape sequence (section 12, item 12). With `debug` on, a multi-line body is logged on one line with `\n` in it.

## 9. Failure sensor

1. Service type `ContactSensor`, `ContactSensorState` open (1) means a failure occurred.
2. Trips according to `failureMode`.
3. Resets to closed on the next fully successful send from that switch, or after `failureSensorResetSeconds`.
4. Exposed on the same accessory as the switch so it shares the room and name.

## 10. Startup validation

Validation runs in code, independent of the schema, and reports every issue in one pass with the field path, for example `switches[2].actions[0].providerId: no provider with id "twillio-main" (did you mean "twilio-main"?)`.

Blocking issues (the platform registers nothing and cached accessories stay): invalid provider credentials format, invalid or duplicate ids, a key named `__proto__`, `constructor` or `prototype` anywhere in the block, a bound of section 12, item 12 exceeded, a switch with no resolvable recipients, a body that violates channel constraints, a provider type that does not serve the requested channel, a switch name that violates HAP naming, an action that references a provider or group that does not exist, and a `defaultProviders` entry that names a provider that does not exist or cannot serve its channel, or a key that is not a channel (section 5.7).

An empty or absent `providers` or `switches` list is not an error, and `config.schema.json` requires neither (only `name`), so the schema form and the runtime agree. A switch with more than one action on the same channel is valid and runs every action, as before 1.1.0; only the custom UI declines to edit it (section 11.2, item 26). No switches means nothing is registered and every cached accessory is removed (section 4, item 9); it is logged as a warning.

Warnings (the platform starts): no providers, no switches, a group with no addresses, a provider that no switch uses, a provider, group or platform name that breaks the display name rule (section 5.2, reported at `providers[i].name`, `groups[i].name` or `platform.name`), a sender not in the provider's list when only one sender exists (it is used anyway), a channel with more than one provider and no `defaultProviders` entry (section 5.7, item 4; the first provider in configuration order is named as the fallback), and a switch that targets a group holding addresses on a channel none of its actions send on (an uncovered channel; logged once per switch and channel with the switch name, the group ids and the channel, because those people receive nothing). From 1.1.0 the custom UI no longer shows the uncovered channel as a warning: its Send by checkboxes make the choice explicit (section 11.2, item 8), so the startup warning is for hand-edited configurations and for channels a user unticked.

Phone numbers without a leading plus sign are normalized using `defaultCountry`; the normalized value is logged once at startup so the user can correct config.json if desired.

## 11. Settings UI

### 11.1 Schema form

`config.schema.json` is complete on its own so the plugin remains configurable if the custom UI fails to load. It uses `headerDisplay` for the getting-started text, section `description` fields, per-field `description` help, `x-schema-form` password types for secrets, and `condition` rules so provider fields appear only for the selected type.

### 11.2 Custom UI

Built with `@homebridge/plugin-ui-utils`. Replaces the schema form with a page of four sections: Providers, Recipient Groups, Switches, and Settings (the platform fields of section 5.1, with Settings > Advanced from item 12 at its end). It reads the platform block with `getPluginConfig`, pushes every change with `updatePluginConfig`, and leaves saving to the Homebridge UI's own Save button, which it only enables or disables; it never calls `savePluginConfig`. Other platform blocks and unknown keys such as `_bridge` are preserved. The page is wrapped in a container with 16px of horizontal padding and every two-column layout is a gap-based grid with no negative margins that stacks to one column below 600px, so nothing is clipped in the settings modal. From 1.3.0 the page is built on the settings shell of item 28, which it shares with the author's other Homebridge plugins.

Additional behavior beyond the schema form:

1. Phone number entry: country dropdown (flag, name, dial code) defaulting to `defaultCountry`, and a national number field with a local placeholder. Nothing is reformatted while the field has focus; it accepts digits, spaces, dashes, dots, parentheses, and a leading plus (other characters are dropped as typed, the caret stays in place). On blur the text is parsed with libphonenumber-js using the selected country as the hint, stored as E.164, the country is re-derived from the parsed number (a +1 305 number selects United States even if Canada was selected) and the dropdown updated, and the field shows the national format. The "Stored as +…" line appears only when parsing succeeds; otherwise an inline error is shown and the raw text is kept. Changing the country re-parses the current text. Applies to every phone field: provider sender numbers, group SMS lists, and extra SMS recipients.
2. SMS body: live character count, segment count, and a warning when a non-GSM-7 character is present with the offending character highlighted.
3. "Test connection" per provider: SMTP runs nodemailer `verify()`; Twilio lists one message (GET `/2010-04-01/Accounts/{accountSid}/Messages.json?PageSize=1` with the API key pair, so Restricted keys scoped to Messaging pass as well as Standard keys); Telegram calls `getMe`; ntfy GETs `/v1/health` and, with credentials, `/v1/account` (section 6.5). Twilio failures include Twilio's error code and message when the response carries them. The SMTP success line reads "Connected to {host}:{port} and logged in."; no Test connection response echoes the submitted username or any credential (the harness asserts it). Credentials from the form are used in memory only and never returned or written.
4. "Test send" per switch: sends the switch's actions to the configured recipients and shows per-recipient results. Requires an explicit confirmation click since it sends real messages (layout in item 11). The UI server validates the submitted block exactly as startup does (including `credentialsFile` against the storage path) before sending, ignores the master switch and the cooldown, and works before Save.
5. "Find people and groups" on the Telegram provider card: calls `getUpdates` (including `my_chat_member` updates, so a group appears as soon as the bot is added to it, before anyone posts) and lists the distinct chats the bot has seen (at most 100, titles cut to 64 characters): first name and username for private chats ("First (@username)"), the title for groups and channels (negative ids). Each row has an Add button that appends the id to the recipient group chosen in the dropdown next to the button; with no group chosen, the button prompts to choose one. A single group is preselected.
6. Switch `id` generation on create: UUID v4 from `crypto.randomUUID`, with a fallback for the non-secure http origins a LAN Homebridge UI usually runs on, where that function is unavailable.
7. Reference pickers: provider and group selectors are dropdowns populated from the current config, not free text.
8. Switch editor (rewritten in 1.1.0; laid out on the shell grid of item 28 from 1.3.0). A switch card's body reads, top to bottom: Name at 12 columns with its help; the Enabled checkbox at 12 with the help "A disabled switch still appears in the Home app but does nothing when turned on."; Cooldown (seconds) and Failure Mode at 6 columns each on one row with their help; the Failure Sensor checkbox at 12 with its help, followed by the Failure Sensor Reset (seconds) field at 6 columns while the sensor is on; then, instead of an actions list, one editor whose parts read top to bottom, its Recipients, Extra recipients and Send by parts each a block with a bold sub-heading and its caption directly under it (before 1.3.0 the name was followed by an ID line, Enabled, Cooldown and Failure Mode shared one row at 4 columns each, and the captions sat under the checks):
   Recipients: one checkbox per configured group, labelled with the group name and its per-channel counts ("Family: 3 SMS, 1 email, 2 ntfy"; "Family: no addresses yet" for an empty group), followed by the block "Extra recipients": one address list per channel a provider can send on (labelled "Phone numbers (SMS)", "Email addresses", "Telegram chat IDs", "ntfy topics"), at 6 columns each (Phone numbers (SMS) and Email addresses on one row, the next lists on the row after; one column below 600px), for people outside the groups. A group a stored action names but that no longer exists is listed ticked in red as "{id} (missing group)".
   Send by: one checkbox per channel present in the selected recipients, that is, a channel on which the ticked groups and the extra recipients resolve to at least one address and a provider can send, side by side at 4 columns each, wrapping as needed (from 1.3.0; stacked before). Each is labelled with the channel and the resolved count ("SMS (3 numbers)", "Email (1 address)", "Telegram (1 chat)", "ntfy (2 topics)"). A channel that becomes present is ticked by default, regardless of which card was edited: ticking a group or adding an extra recipient on the switch card, giving a group its first address on a channel on the group card, or adding a provider for a channel the recipients already have on a provider card all tick it. The comparison of present channels before and after a change lives in one place that every section's changes pass through (the page's change handler), not in each card. A channel the user unticked by hand stays unticked while it stays present (an unrelated edit, or a second address on that channel, does not tick it) and is ticked again only if it leaves and comes back. Unticking one skips it for this switch (no action is written for it). A channel with nobody to reach is not listed and gets no action, and neither is a channel nobody can send through unless the switch already has an action on it. That ticked channel no provider serves (a stored action whose provider was removed, or whose only provider stopped serving the channel; section 5.7, item 7) stays listed, ticked and drawn as disabled, with the note "No provider configured for {channel}; add one or untick to remove." under it ({channel} reads SMS, email, Telegram or ntfy). The box stays usable: unticking it drops the action and the row. Until then the stored action, `providerId` included, is written back unchanged, and the channel takes no part in the preview line or in Advanced. While nothing is present the line "Pick a group or add an extra recipient to choose how to send." shows instead.
   Message: one "Message" body field for every channel, with the SMS counter, help and placeholder of item 2 while SMS is ticked and the plain text help otherwise, and one "Subject" field, shown only while email or ntfy is ticked, with the help "Used as the email subject and the ntfy title. Defaults to the switch name." Both carry the Variables toggle of item 17.
   Preview: a live line under the message, "Will send SMS via Twilio to 3 numbers, email via Fastmail to 1 address, ntfy via ntfy to 2 topics." (the provider's display name, else its id), or "Nothing will be sent yet." while no ticked channel reaches anyone.
   Advanced (a collapsed disclosure, opened on load when anything in it is set; a second 12-column grid from 1.3.0, item 28): first the switch id as a read-only 12-column field labelled "ID" with the caption "Generated. HomeKit tracks the switch by this id, so you can rename it freely." (moved here from the body in 1.3.0; it has no Edit link, and an issue on it opens the disclosure as for any field); then the checkbox "Customize message per channel", which hides the shared Message and Subject, shows the note "Each channel has its own message under Advanced." in their place, and expands one block per ticked channel with its own message ("SMS message", "Email message", "Telegram message", "ntfy message") and, for email and ntfy, its own "Email subject" or "ntfy title", each prefilled from the shared fields when the box is ticked; a provider dropdown per ticked channel at 6 columns ("SMS provider", "Email provider", "Telegram provider", "ntfy provider"), shown only for a channel with more than one provider (or one whose stored provider no longer exists while another provider serves the channel, listed as "{id} (missing)"), whose first option is "Platform default ({name})" (section 5.7) with the help "For this switch only. The default for every switch is under Settings."; the SMS Sender dropdown of section 5.5, item 3, at 6 columns, when the resolved Twilio provider offers more than one sender or a sender is stored; the ntfy Priority and Tags fields at 6 columns each on one row while ntfy is ticked; and the "Hide recipients from each other (BCC)" checkbox at 12 columns while email is ticked (that order from 1.3.0; before, BCC came before the ntfy fields, at 4 and 8 columns).
   Writing: on every change the editor writes the switch's `actions` array (section 5.4): one action per ticked channel, in the order the stored actions had with new channels after in channel order, each with `providerId` from the override or the platform default, `groups` from Recipients, `recipients` from that channel's extra list, `body` and `subject` from the shared fields or, with Customize on, the channel's own, `bcc` for email, and `priority` and `tags` for ntfy. Loading reverses it: the first action per channel fills that channel, the union of the actions' groups fills Recipients, identical bodies (and subjects) collapse to the shared fields, differing ones open Customize per channel, a provider that is the channel's resolved default loads as no override and any other as an override. A switch with more than one action on the same channel cannot be read into the editor; the page then shows nothing of the configuration and falls into the state of item 26. The round trip is lossless for a configuration written by the editor or by hand with one action per channel, an action whose provider is gone included; the harness loads every fixture under `test/fixtures`, renders it, writes it and compares. Actions of one switch that target different groups load with the union and are written with it.
   Validation (item 15) lives on the editor's fields: "Pick at least one group, or add an extra recipient." on Recipients (also revealed by the Name field) while nothing is present, "Turn on at least one channel." on Send by while every present channel is unticked, a missing group or a channel reaching more than 100 addresses on the group checkboxes or the channel's checkbox, the note of a ticked channel no provider serves on that channel's checkbox (it blocks Save, as startup would refuse the action; the note is the message, so nothing is repeated under the box), the provider checks (missing, wrong type, a Twilio provider without a from address, the SMS sender rules) on the channel's provider dropdown or the Sender dropdown, the body rules of section 5.5 on the shared Message (checked against every ticked channel) or on each channel's own message, and the tag rules on Tags. The uncovered channel warning and its "Add … action" button of earlier versions are gone; they are unreachable, because an unticked channel is a choice. Test send (item 11) counts the distinct recipients across every ticked channel and is gated by issues on the switch, on the providers its ticked channels resolve to, and on its groups; its results render in the card's result bar (item 28).
9. Twilio "Look up numbers": a button on the Twilio provider card, enabled once `accountSid`, `apiKeySid` and `apiKeySecret` are filled. Through the UI server it calls GET `/2010-04-01/Accounts/{accountSid}/IncomingPhoneNumbers.json?PageSize=20` and GET `https://messaging.twilio.com/v1/Services?PageSize=20` with the same Basic auth. The result is a dropdown of phone numbers with their friendly names and a separate dropdown of Messaging Services; choosing a number appends it to the senders list (once), choosing a service fills the Messaging Service SID field. When either response reports more than the page holds, the status line ends with "Showing the first 20; enter others manually." When the key may not list numbers (HTTP 401 or 403) the status line reads "This API key cannot list numbers. Enter them manually." (Messaging Services are still listed when that call succeeds). Manual entry remains available. The Messaging Service SID field sits under the card's "Advanced" disclosure together with the credentials file, and the disclosure opens when either is set. From 1.3.0 the Advanced grid (item 28) puts the ID and the Messaging Service SID on one row at 6 columns each and the credentials file at 12; the API Key SID placeholder reads "e.g. SK…", the Email From address and name placeholders "e.g. alerts@example.com" and "e.g. Home".
10. Telegram onboarding: the Telegram provider card is a three-step guided flow (copy in section 11.3) followed by Find people and groups. Step 1 "Create your bot" has an intro sentence, an "Open BotFather" link button to `https://t.me/BotFather`, a QR code of the same link captioned "Scan to open BotFather", four numbered instructions, and the bot token field; once the token matches the token pattern the UI server calls `getMe` and the card shows "Connected to @{username}" or the error. Step 2 "Choose how people receive messages" has two selectable cards, "Family group chat (recommended)" and "Individual chats", with no links or QR codes inside them; the choice drives step 3 and the two variants are never shown together. With the group card selected, step 3 is titled "Add the bot to your group" and shows a QR code of `https://t.me/{username}?startgroup=true` captioned "Scan to add the bot to a group", the link as text, an "Add bot to a group" link button, a "Copy link" button and the group sentence from section 11.3. With individual chats selected, step 3 is "Invite people": a QR code for `https://t.me/{username}?start=join` captioned "Scan to start receiving alerts", the link as text, an Enlarge button (full-page modal), "Copy link" and "Copy invite message". Links and QR codes appear once `getMe` succeeded and only embed the bot username, which the UI server limits to `^[A-Za-z0-9_]{5,32}$` and the page checks again before building a link; before that step 3 shows "Connect your bot in step 1 to get the links and QR codes." Step 4 is "Find people and groups" (item 5); once it returns at least one result the line "Added people appear in the group's Telegram chat IDs list. Save when you're done." appears under the results. Parse Mode sits under the card's Advanced disclosure (item 14) and defaults to plain text; from 1.3.0 the Advanced grid (item 28) puts the ID and Parse Mode on one row at 6 columns each and the credentials file at 12, the field and its help unchanged. QR codes are generated as inline SVG in the UI bundle by `qrcode-generator`, a dev-time dependency; the plugin has no new runtime dependency. On phones and touch devices the QR codes give way to link buttons (item 16).
11. Card button layout: each switch card has a footer strip (item 28) with "Remove switch" as a red text button on the left, the text button "Duplicate switch" beside it (from 1.2.0), and "Test send" as the only outlined primary button on the right, drawn as a 31px outlined button in the link colour from 1.3.0. Duplicate switch creates a copy directly below the source with a new UUID (item 6), the name "{name} copy" (then "{name} copy 2", "{name} copy 3", each within the 64-character cap, the source name cut to make room; "Copy" when the source name is empty) and everything else identical: actions, recipients, messages, subject, Advanced settings, cooldown, failure mode, failure sensor and enabled state. The copy is treated as new (the touched and status handling of Add switch, item 15), its Name field gets focus and the card scrolls into view. Clicking Test send replaces the button in place with "Send to {n} recipients now?" ({n} is the number of distinct recipients across the switch's ticked channels, "recipient" when it is 1), a primary "Send" button and a text "Cancel" button; Escape or Cancel restores the original button. Test send is disabled, with a short hint beside it, while the switch or a provider or group it uses has a validation issue ("Fix the errors above first") or while the switch resolves to zero recipients ("No recipients yet"), so the confirmation can never read "Send to 0 recipients". Per-recipient results render in the card's result bar, between the body and the footer strip (item 28; below the footer before 1.3.0), with a Dismiss link. Provider cards use the same footer ("Remove provider" left, "Test connection" right, the result in the same bar with the same Dismiss link) and group cards too ("Remove group" left with the text button "Duplicate group" beside it from 1.2.0, nothing on the right). Duplicate group follows the same pattern: a copy directly below the source named "{name} copy" with the same suffix rule (the display name cap of section 5.2), its id generated from the new name with `uniqueSlug` and following the name until edited (item 14), every address list copied, and no switch changed; the copy is treated as new, its Name field gets focus and the card scrolls into view. "Remove switch", "Remove provider" and "Remove group" confirm in place the same way: the text button is replaced by "Remove {title}?", where {title} is the card's current name inserted as text, or "Remove this switch?" (or provider, or group) while the card has no name (from 1.3.0; before, always the noun), a red 31px "Remove" button and a text "Cancel" button; Escape or Cancel restores it. List-entry Remove buttons (phone, email, chat id, topic) stay single-click. Outlined secondary buttons keep their outline on hover and focus with a light background highlight (an 8% grey wash) that does not read as pressed; a press (active) is a shade darker (20%), and neither ever takes a solid fill. No two primary buttons are ever adjacent in a card footer; the layout smoke test asserts it. The summary box (item 15) lists its entries left-aligned with the bullet beside the first line of each entry's text, wrapped entries included; the layout smoke test asserts it at 360px.
12. Settings > Advanced: a disclosure at the bottom of the Settings section, collapsed by default, with four actions. "Download backup" exports the current platform block as `notify-switch-backup-YYYY-MM-DD.json` (local date). "Download backup without credentials", next to it in the same row, exports the same JSON as `notify-switch-backup-without-credentials-YYYY-MM-DD.json` with every secret field replaced by an empty string and a top-level `"credentialsRemoved": true`. The secret fields are `apiKeySecret`, `password`, `botToken` and, on an ntfy provider, `token` with `auth` `token` or `password` with `auth` `basic`, and, on a provider with a `credentialsFile`, every key that file may supply (section 12, item 2: `accountSid`, `apiKeySid` and `apiKeySecret`; `username` and `password`; `botToken`; `token`, `username` and `password`), since those values are credentials too; the `credentialsFile` path itself stays. The help line under the buttons is in section 11.3. "Restore from backup" is a file picker; a file over 1 MB is refused before it is read ("The file is larger than 1 MB, which a Notify Switch backup never is."); otherwise the file is parsed as JSON, refused if a key named `__proto__`, `constructor` or `prototype` appears at any nesting level ("The file contains a key named \"{path}\", which is not allowed."), accepted as either a platform block or a whole config.json holding one, and checked against the same rules the form applies (which mirror `config.schema.json` and startup validation) before anything changes; failures are listed under the picker; on success the form state is replaced and Save is enabled. A file with `credentialsRemoved: true` is accepted with its secret fields empty: every other issue still rejects it, the marker is dropped so it never reaches config.json, everything else is loaded, and the emptied secret fields are marked touched (item 15) so their errors show inline and the issue list names them; Save stays disabled until they are filled in. "Reset plugin to fresh install" is a red text button that opens a confirmation modal listing what is removed (all providers, groups, switches, and settings; switches disappear from the Home app after the next restart; credentials files on disk are not touched) with a "Download backup first" button, a text field that must contain `RESET`, and a red Confirm button that is disabled until it does. Confirming replaces the form with the empty default configuration (`platform`, `name`, `configVersion`, empty `providers`, `groups` and `switches`; the exported block also carries the default `defaultCountry`, `masterSwitch` and `debug` values) and enables Save. Startup behavior after saving is in section 4, item 9.

13. Provider chooser: "Add provider" is replaced in place by a chooser: the prompt "Which service should send your messages?", four tiles (Twilio; Email (SMTP); Telegram; ntfy), each with a title and one line of help (copy in section 11.3), and a "Cancel" outlined secondary button that restores the button. The tiles fill the width in equal columns of at least 150px each (an auto-fit grid: four across on a desktop, wrapping to further rows as the width shrinks) and stack to one column below 600px (from 1.3.0; before, fixed at four, two and one columns). "Add provider" is the section's 38px primary button (item 28). Picking a tile creates the card with its type fixed (there is no Type dropdown; the type badge stays in the card header and reads Twilio, SMTP, Telegram or ntfy), the name prefilled (Twilio, Email, Telegram, ntfy; from 1.2.0 with a numeric suffix when another provider already uses the name, "Twilio 2", then "Twilio 3", within the 64-character display name cap) and the id generated from that name (twilio, email, telegram, ntfy; `twilio-2` for "Twilio 2", or with a numeric suffix of its own when the id alone is taken).
14. Hidden ids: provider and group ids are generated from the name and never shown in the main form. Each card's "Advanced" disclosure (the "▸ Advanced" line of item 28, opening a second 12-column grid) holds the ID read-only with an "Edit" text button that makes it editable, for people who hand-edit config.json; one-sentence help reads "How switches refer to this provider in config.json." (or group). Format and uniqueness are still validated with the same messages, and an issue on the ID opens the disclosure so it is never invisible. A generated id keeps following the name as it is typed until it is edited by hand or a switch action refers to it, so renaming a provider or group never breaks a switch. A new group starts as `group` (then `group-2`) until it is named. Switch ids stay UUIDs (item 6) and are shown read-only, with no Edit link, first in the switch card's Advanced grid (item 8; a line in the body before 1.3.0).
15. Per-field validation. Touched state is tracked per field, not per card: a field is touched when its control loses focus, when a select, checkbox or radio in it changes, or when its entry in the issue list is used to jump to it (typing alone does not count). A field shows its error inline only once it is touched. Cross-field checks belong to every field they read and appear inline once any of those fields is touched: "Pick at least one group, or add an extra recipient." to the switch's Recipients, its extra recipient lists and its Name field, "Turn on at least one channel." to Send by and Recipients, a duplicate switch name to both name fields, a duplicate or invalid id to the card's Name field as well, and a sender check to the channel's provider dropdown too (item 8). While a field has an error showing, input re-validates only to clear it; an error is never added while the field has focus, it appears when the field is left. This applies to every field, list entries (phone numbers, email addresses, chat ids, topics) and the switch editor's fields included. A touched field that passes validation and holds a value shows a small green check inside the control. Validation messages render on their own line below the field or list row, never in a flex row with inputs or buttons, and an input never changes width or wraps when a message appears or disappears; a browser test types into an email entry and a phone entry and asserts the input's bounding box is unchanged after the error appears. A provider, group or switch added in this session is fresh: its card shows placeholders and no errors, and its issues are left out of the issue list, until a field in it is touched. Save is disabled while any issue exists, fresh cards included; while every remaining issue is on a fresh card the issue box shows the info line "Fill in the new provider to enable Save." (or group, or switch, or "provider and group") instead of the list. Otherwise the box "Fix these before saving:" lists one entry per field, each a link that scrolls to the field, marks it touched and focuses it; with more than three entries the box collapses to the one-line count "{n} fields need attention" with a "Show all" toggle ("Hide" once expanded). The Homebridge UI disables its Save button while issues exist, so a Save attempt cannot reveal the errors; touching a field, or the issue list, is the trigger. Placeholders are examples, never values: every realistic-looking placeholder starts with "e.g." ("e.g. Family", "e.g. Water Leak Alert", "e.g. Home"), placeholders are italic in a visibly lighter shade than typed text in both themes (the colour is the host's own placeholder colour, which the Homebridge UI sets per theme with `!important`; the plugin adds only the italics, and the layout test asserts at least 2:1 between the placeholder colour and the input's text colour), and where a field has a help line it carries an example too ("For example: Family, Neighbors, On-call.").
16. Phone-friendly layout: below 600px a phone row stacks, the country dropdown on its own line and the number field with Remove beside it on the next. On viewports below 600px or on touch devices (`(pointer: coarse)`, or `navigator.maxTouchPoints > 0`, which sets `ns-touch` on the body) QR codes, their captions and Enlarge are hidden and "Open in Telegram" (opens the t.me link), "Copy link" and "Share" (Web Share API, hidden when the browser has none) are shown instead. Card footers put the primary action first in the markup and reverse the row, so when a footer wraps the primary action is the top line and Remove drops below it. Every button, text buttons included, is at least 44px tall on touch devices. Each card header has a "Show help" / "Hide help" text button that collapses field help (status lines, counters, the SMS preview and validation messages stay visible); help is expanded at 600px and above and collapsed below, and the choice is remembered per card in memory for the page's lifetime only. The layout smoke test runs at 900, 599, 400 and 360px, asserts no horizontal overflow and stacked phone rows at 360px, the primary action on top of a wrapped footer, 44px targets on a touch device, and dark-mode contrast (item 18).
17. Message fields: every body and subject field (the shared ones and the per-channel ones under Advanced) has a link-styled "Show variables" / "Hide variables" toggle with a chevron on its label row that toggles a list of `{{switchName}}`, `{{time}}`, `{{date}}` and `{{datetime}}` with a "More about variables" link to the README; the field help no longer lists variables (the schema form, which has no toggle, keeps the list in its `body` and `subject` descriptions). From 1.2.0 each entry is a real button (keyboard accessible) showing the token, with the value it would render right now in secondary text beside it: `{{time}}`, `{{date}}` and `{{datetime}}` in the platform's current Time format and Date format settings (item 27), `{{switchName}}` as the switch's current name, or "Switch name" while it is empty; the values are read each time the list opens and inserted as text. Clicking an entry inserts the token at the caret of the field the list belongs to (replacing any selection), places the caret after the token, returns focus to the field and fires the field's `input` event, so the same change handling runs as for typing. Under the list one help line reads "Click a variable to insert it at the cursor."; the "More about variables" link stays. There is no Channel dropdown any more: the channels a switch sends on are the Send by checkboxes of item 8. The ntfy Priority dropdown and Tags text field (comma separated; the model keeps an array) sit under the switch's Advanced disclosure while ntfy is ticked. Field help is one sentence; where more is needed the sentence ends with a link to the matching README section, usually "Where do I find this?" (section 11.3 lists the links).
18. Theme contrast: every piece of secondary text (field help, hints, empty-state lines, captions, table captions, disabled buttons, every `details > summary` disclosure label such as Advanced and Common settings, the help toggle) is styled by one rule that takes the Bootstrap theme variable for secondary text, `--bs-secondary-color`, with `currentColor` (the surrounding text colour, which the host sets for its theme) as the fallback, never a fixed grey. Borders, step badges and help links use `--bs-border-color`, `--bs-secondary-bg` and `--bs-link-color` the same way. The Homebridge UI marks dark mode inside the settings iframe by adding `dark-mode` and `config-ui-x-dark-mode-{theme}` to the body and colouring the body and cards itself; it does not set Bootstrap's `data-bs-theme` on the root, so Bootstrap's variables keep their light values there. Under that body marker the stylesheet re-declares the variables it reads with Bootstrap's dark values, so the one rule serves both themes. The host's dark marker also paints every `.btn-link` grey with `!important`; from 1.3.0 one override rule for `.btn-link` inside the page gives text buttons their intent colour back (Remove and Reset stay `--bs-danger`, links stay `--bs-link-color`, the help toggle stays the secondary colour, summary box entries keep the surrounding colour), every alert box (the default-provider prompt, the draft banner, the summary box, result messages) is left-aligned with `text-align: start` regardless of the host's alert styling, and the variable tokens in the Variables list are drawn in the link colour so they read as clickable (item 28). Read-only, locked and disabled controls (from 1.2.0): every read-only, locked or disabled input, select and textarea (the ID field under Advanced, the Host, Port and Security fields locked by an SMTP preset, and any other, such as the whole page under the upgrade notice of item 26) draws with `--bs-secondary-bg` behind the surrounding text colour (`color: inherit`, which is the body text colour the host sets for its theme) in one rule of `homebridge-ui/public/index.css`, in both themes; under the dark marker that is Bootstrap's dark secondary background behind light text, never a light background with dark text. The headless layout test renders the page the way the host does (its body classes and the host's own body, card, alert, link and button rules; no `data-bs-theme`) in dark mode and light mode, on a full configuration and on an empty one, and asserts WCAG AA (4.5:1) contrast against the effective background for every read-only, locked or disabled input and select (with an SMTP preset locking its server fields and every Advanced disclosure open; in dark mode their background must be dark too) and for at least one help element per card type, the Advanced label, a QR caption, a help link, the help toggle and the footer, and on the empty configuration for an empty-state line, checkbox help, field help under Settings, the Settings > Advanced summary toggle, the Get started card's caption and tile help, the "Add a provider first." hint, a disabled Add button and the Save status line.
19. Guided empty state: while the configuration has no providers, the Providers section body is a single "Get started" card (the section intro and the Add provider button are not shown) holding the line "Choose how you want to send messages. You can add more providers later." and the provider chooser tiles from item 13 inline; picking a tile creates the provider exactly as the chooser does. The Recipient Groups and Switches sections stay visible with their intros and empty-state lines, but their Add buttons are disabled, drawn as outlined 38px buttons in the secondary text colour with `cursor: not-allowed` (item 28), with the hint "Add a provider first." beside them (and as the button's tooltip). The Get started card holds the chooser tiles inline with no Cancel button. Once one provider exists the page renders as before, and removing the last provider brings the empty state back. The Save status area (the sticky box that otherwise lists issues or the "Fill in the new …" line) reads "Nothing to save yet" while there are no providers, groups or switches and no issues, except immediately after Reset plugin to fresh install (item 12), where it reads "Configuration reset. Click Save, then restart Homebridge." until the next change. Save is enabled in both cases, because an empty configuration is valid.
20. Version and credit footer: the last element of the page (the page banner of item 28 is the first) is one line of secondary text with a 1px top rule, wrapping between its items on narrow screens: "Notify Switch v{version} · Made by Alex Rodriguez · alex-rodriguez.com · Report an issue". The brand mark (`assets/notify-switch-mark.svg`, inlined into the UI bundle at build time, no file is loaded) precedes the plugin name at 20px; it is drawn with `currentColor` so it follows the theme, and is hidden from assistive technology. It appears nowhere else on the page. The version is the installed package's, read from package.json by the UI server (`/version`), so it always reflects the installed package; if the server does not answer, the line reads "Notify Switch" without a version. "alex-rodriguez.com" links to `https://alex-rodriguez.com/?ref=notify-switch#building` and "Report an issue" to `https://github.com/arodbuilds/homebridge-notify-switch/issues`; both open in a new tab (`rel="noopener noreferrer"`). There is no tracking beyond the `ref` parameter.
21. Default country from locale and host time zone: on first load, when the stored platform block has no `defaultCountry` (a fresh install), the Default Country dropdown is prefilled from the region of `navigator.language` (`en-GB` gives GB, `pt-BR` gives BR) when it is a country the phone entry knows; else from the Homebridge host's time zone, asked from the UI server (`/host-timezone`, which returns `Intl.DateTimeFormat().resolvedOptions().timeZone`) and mapped to a country with a small static table (`Europe/Berlin` gives DE; a zone the table does not know, or one shared by several countries such as `UTC`, gives nothing); else US. The host is asked only when the locale names no region. The value is written with the next Save like any other default. A saved `defaultCountry` is never overridden. Reset plugin to fresh install keeps the default `US` (item 12). Startup normalization (section 10) is unchanged and uses the saved value only.
22. SMTP preset picker: the SMTP card opens with a "Mail provider" selector: Fastmail, Gmail, iCloud, Outlook.com, Yahoo, Zoho, Other, as a segmented control on wide screens and a dropdown below 600px, text labels only. Choosing a preset fills host, port and security (Fastmail: smtp.fastmail.com, 465, SSL. Gmail: smtp.gmail.com, 465, SSL. iCloud: smtp.mail.me.com, 587, STARTTLS. Outlook.com: smtp-mail.outlook.com, 587, STARTTLS. Yahoo: smtp.mail.yahoo.com, 465, SSL. Zoho: smtp.zoho.com, 465, SSL) and locks the three fields, with an "Edit" link on the Host label row that unlocks them; Other leaves them editable. Host, Port and Security share one row at 7, 2 and 3 columns (from 1.3.0; 6, 3 and 3 before) with their help line under the whole row, not under one column; the card's Advanced grid (item 28) holds the ID and the credentials file side by side at 6 columns each. From 1.2.0 the provider name follows the preset while it is still one the UI filled in: when the name is the chooser's "Email" or a preset label (either with or without the numeric suffix of item 13, "Gmail 2"), choosing a preset renames the provider to the preset label (Fastmail, Gmail, iCloud, Outlook.com, Yahoo, Zoho; Other goes back to "Email"), with a numeric suffix when another provider already uses that name, and the id follows per item 14. A hand-typed name is left alone; the rule is by value, so a typed name that happens to equal a preset label follows the preset too. The password help names the chosen provider and links directly to its app-password page (copy in section 11.3); the Gmail text notes that 2-Step Verification must be on first. The preset key is stored as `smtpPreset` for redisplay only: on load the stored host, port and security are shown locked when a preset is stored and are never rewritten; the runtime reads host, port and security. There is no "Common settings" table any more.
23. Unsaved draft recovery: on every change the in-progress platform block is written to `localStorage` under `homebridge-notify-switch:draft` with a timestamp. On load, when a draft exists that differs from the configuration about to be shown and is less than 24 hours old, a banner at the top of the page, directly under the page banner of item 28, reads "You have unsaved changes from earlier. Restore them?" with a primary 31px "Restore" button and an outlined 31px "Discard" button (item 28; the copy is unchanged from 1.2.0). Restore loads the draft into the form and marks every field touched (item 15); Discard deletes it. A draft equal to the saved configuration (the changes were saved) is deleted on load, so a successful Save clears it at the next open. A Reset confirm (item 12) deletes the draft and writes none until the next change. The Homebridge UI offers no hook to intercept Close, so the banner is the only recovery path. A draft over 1 MB is never written and is discarded on load, and so is one holding a key named `__proto__`, `constructor` or `prototype` at any level (section 12, item 12).
24. ntfy card (from 1.1.0): the intro sentence from section 11.3 with a "Where do I find this?" link to the README's ntfy section, the Server field (default `https://ntfy.sh`, monospace), an Authentication dropdown (None; Access token (recommended); Username and password) whose help line follows the choice, then only the credential fields the mode needs (the access token as a password field, or username and password side by side), and the Advanced disclosure with the ID and the credentials file side by side at 6 columns each (from 1.3.0, item 28). Test connection sits in the footer like every provider card. Group cards gain an "ntfy topics" list under the Telegram chat IDs with the help from section 11.3; topic entries take the plain keyboard and the placeholder "e.g. home-alerts-x7q2". From 1.3.0 the group card's Phone numbers (SMS) list carries the caption "Stored with the country code from Settings." under it, and its Advanced grid (item 28) holds the ID at 6 columns; everything else on the group card is as before.
25. Platform defaults (section 5.7; rewritten in 1.2.0). Written on appearance: the moment a second or later provider for a channel validates (its card has no error) and `defaultProviders[channel]` names none of the channel's providers, the UI writes the current fallback, the first validated provider in configuration order, to `defaultProviders[channel]`, so the configuration is fully determined even if the prompt is never answered. The comparison runs on every validation pass in one place (the page), whichever card changed. The Settings dropdown shows the written value; there is no summary-box warning and no "Optional before saving:" status for a missing default any more.
   The prompt: the card of the last validated provider for the channel in configuration order (the one just added) shows, at the bottom of its body, directly above the footer row (Remove provider / Test connection), one prompt per such channel: "You now have {n} ways to send {channel}. Switches use {defaultName} unless told otherwise. Which should they use?" where {defaultName} is the display name (else id) of the provider currently written as the default, then a radio list of every validated provider for the channel (name and type badge) with the card's own provider preselected, then two buttons on one row: the primary "Use the selected provider" and the text button "Keep {defaultName}". A radio only selects; nothing is written and the prompt does not close until a button is pressed. "Use the selected provider" writes the selection and closes the prompt; "Keep {defaultName}" closes it without changing the entry. A card can show one prompt per channel it serves (a Twilio provider with a from address can ask for SMS and for email). The prompt shows only on the load in which the entry was written (the provider was added, or a hand-edited configuration without an entry was opened); on a later load an already-written entry shows no prompt. Choosing under Settings or with Make default (below) also closes the prompt for that channel.
   Badge: on every provider card header, after the type badge in the header's left cluster, for each channel the provider is the resolved default of (the written entry, or the sole-provider default of section 5.7, item 2, as long as more than one provider serves the channel) a status badge "Default for {channel}" on the success fill (item 28; a secondary badge before 1.3.0); on a card that serves such a channel but is not its default, the 12.6px link "Make default for {channel}" in the header's right cluster, before the help toggle (item 28; in the badge's spot before 1.3.0), which writes the entry. Nothing is shown for a channel with one provider. {channel} reads SMS, email, Telegram or ntfy. Every provider name in the prompt and the header is inserted as text, never as HTML.
   The Settings section shows a "Default {channel} provider" dropdown per channel with more than one provider, only while more than one exists, with the help "Switches send {channel} through this provider unless a switch says otherwise under Advanced."; it shows the written value, redraws when providers are added or removed, and shows the placeholder "Choose a provider…" (not selectable) only while nothing is written yet, that is, while the second provider is still being filled in. Removing the default provider follows section 5.7, item 6: one left, it is the default silently; more left, the new fallback is written at once and the prompt comes back on the last remaining validated card.
26. Unsupported legacy configurations (from 1.1.0). On load, when any switch of the stored block holds more than one action on the same channel (a 1.0.x action list, or a hand edit), the custom UI does not attempt to represent the configuration. The first element of the page after the page banner (item 28; the very first before 1.3.0) is a blocking notice (`role="alert"`): "Warning: upgrading to 1.1 requires reconfiguring this plugin. Download a backup for reference, then use Reset plugin to fresh install under Advanced and set up your switches again." The Switches section shows the line "Not shown: {names}. This switch has more than one action on the same channel, which this version cannot edit." ("These switches have" for several) in place of the cards; the Providers, Recipient Groups and Settings sections are drawn from the block as usual, and every control on the page is disabled (disclosures still open) except "Download backup", "Download backup without credentials" and "Reset plugin to fresh install"; Settings > Advanced opens on load so they are in view. Restore from backup is disabled with the rest. Nothing is pushed through `updatePluginConfig`, no draft is written or offered, the Save status area is hidden and Save is disabled. Both backups carry the block exactly as `getPluginConfig` returned it, the second with its secret fields emptied as in item 12 and `credentialsRemoved: true`, so every action is in the file. Confirming Reset (item 12) replaces the configuration with the empty one, removes the notice and the lock, and the page behaves as after any Reset: the empty configuration is pushed, Save is enabled, the status area reads the reset line. Startup validation is unchanged: the runtime still supports several actions per channel, so such a configuration keeps sending as before until it is reconfigured (section 10). Restore from backup, on an ordinary page, refuses such a file with "The file has more than one action on the same channel on \"{name}\", which this version cannot edit. Set the switch up again instead." and leaves the form alone.
27. Time and date formats (section 5.1, from 1.2.0). The Settings section shows two dropdowns directly below Default Country: "Time format" with the options "12-hour (5:15 PM)" and "24-hour (17:15)", and "Date format" with the options "Month/Day/Year (9/8/2026)", "Day/Month/Year (8/9/2026)" and "Year-Month-Day (2026-09-08)", with one help line under the pair: "Used by {{time}}, {{date}} and {{datetime}} in messages." Both values are written with the next Save like Default Country (a missing field shows and writes the default), Reset plugin to fresh install keeps the defaults, and the page's own validation treats each as a closed enum. The schema form carries the same two fields as enums with those titles and one-line descriptions.
28. Shell (from 1.3.0). The page is built on one settings shell shared with the author's other Homebridge plugins (the Claude Design handoff of September 12, 2026, "Plugin Settings Standard"); a change to any rule below must keep the page identical to the other plugins. Colours stay the host's Bootstrap variables with the fallbacks the stylesheet already uses (item 18); the plugin owns no palette. Sizes are in CSS pixels at the host's 14.4px body size (Bootstrap's `.form-text` at `.875em` gives the 12.6px captions, `.btn-sm` the 31px buttons, the default `.btn` and `.form-control` the 38px controls).
   Page banner: the first element of the page, above the draft banner (item 23) and the upgrade notice (item 26), is `assets/notify-switch-banner.png` at full container width with a 4:1 aspect ratio, a 6px radius and a 16px top margin, with the alt text "Notify Switch, Homebridge switches that send SMS, email, Telegram, or ntfy messages when turned on." It is served from `homebridge-ui/public/` beside the bundle (the build copies it there; the copy is gitignored and published with the package) and referenced by a relative path; nothing on the page loads from an external host, and the harness asserts both. Below 600px it stays full width.
   Draft recovery banner: "Restore" is a 31px primary button and "Discard" a 31px outlined button (item 23).
   Buttons: every button has a 4px radius. Every secondary action is a small button, 31px tall with 13.5px uppercase text: the list Remove buttons, the outlined Add buttons on lists (Add phone number, Add email address, Add chat ID, Add topic, Add sender number), Look up numbers, Open BotFather, Find people and groups, Download backup, Download backup without credentials, the in-place Remove and Send confirms, and the card footer's primary action (Test connection, Test send), which is an outlined button in the link colour (`--bs-link-color`, not the host's primary fill). The password field's attached Show / Hide button carries the same lettering but keeps the height of the 38px control it is joined to. Text buttons (Remove {noun}, Duplicate {noun}, Make default, Show help, Edit, Cancel, Dismiss, Keep, the variables toggle) keep their case and size, as does the Mail provider segmented control, which is a selection. The section Add buttons (Add provider, Add group, Add switch) are the one 38px primary button of their section, with uppercase 16px text. Touch devices keep the 44px minimum of item 16 on every button.
   Card: 1px border, 6px radius, 16px below, `overflow: hidden`. Header strip: the subtle fill (`--bs-card-cap-bg`, re-declared as `#262626` under the dark marker), 8px 16px padding, 1px bottom rule. Left cluster: the bold title (live from the Name field; "New provider", "New group" or "New switch" while unnamed), the type badge (11.5px weight 600, 3px 7px padding, 4px radius, the `--bs-secondary` fill with white text; provider cards only) and, when set, a status badge on the `--bs-success` fill with white text reading "Default for {channel}" (item 25). Right cluster, 12.6px links with a 12px gap: "Make default for {channel}" in the link colour (only on a provider that can be made a default), then "Show help" / "Hide help" in the secondary colour (item 16). The clusters align on their first baseline, so below 600px the badges wrap under the title while the right cluster stays on the first row. Body: 16px 16px 0 padding; the 12-column grid with an 8px column gap and 16px under each field, every cell full width below 600px. Advanced is a disclosure line reading "▸ Advanced" closed and "▾ Advanced" open (the glyph is drawn by the stylesheet; the label stays "Advanced"), 12.6px in the secondary colour, under the grid, opening a second 12-column grid whose cell widths each card gives (items 8, 9, 10, 14, 22 and 24). Result bar: the Test connection or Test send result renders between the body and the footer strip as a 12px 16px row with a 1px top rule holding the message (or the per-recipient table) and a "Dismiss" link; empty, it takes no space. Footer strip: 8px 16px on the subtle fill with a 1px top rule; left, the danger text button "Remove {noun}" whose in-place confirm reads "Remove {title}?" with the card's current name inserted as text, or "Remove this {noun}?" while it is unnamed, with a red 31px "Remove" button and a "Cancel" link, then the "Duplicate {noun}" link on group and switch cards; right, the outlined primary action, none on group cards (item 11).
   Gating and empty state (item 19): while no provider exists, Add group and Add switch are outlined, disabled, `cursor: not-allowed`, in the secondary text colour, with "Add a provider first." beside them; the Providers section shows the Get started card with the chooser tiles inline and no Cancel. Chooser tiles are an auto-fit grid with a 150px minimum, one column on phones (item 13).
   Sticky summary box (item 15): unchanged in behaviour, and it stays at the bottom of the scroll area in both themes; its `position: sticky` rule is prefixed with the page's container class so it wins over Bootstrap's `.alert { position: relative }`, which the host loads after the plugin's stylesheet (before 1.3.0 that rule lost and the box scrolled with the page).
   Dark mode (item 18): text buttons keep their intent colours under the host's dark marker through one override rule for `.btn-link` inside the page; the default-provider prompt and every alert box are left-aligned; the variable tokens in the Variables list are in the link colour.

### 11.3 In-app copy

All copy is final in the spec so it can be reviewed before implementation. Field help is one sentence, two at most; anything longer is a link to the README section named, shown after the sentence as "Where do I find this?" unless another link text is given. README links point at `https://github.com/arodbuilds/homebridge-notify-switch#{anchor}`; the harness checks that every anchor used is a heading in README.md.

Page banner (section 11.2, item 28): alt text "Notify Switch, Homebridge switches that send SMS, email, Telegram, or ntfy messages when turned on."

Getting started (top of page, two paragraphs):

"Notify Switch adds switches to the Home app. Turn one on, usually from an automation, and it sends a message, then turns itself off."

"Set up in three steps: add a Provider (the service that sends), create a Recipient Group (who receives), then create a Switch (what to send). You only need one provider. Save, restart Homebridge, and add the switch to a HomeKit automation."

Providers section:

"A provider is the service that delivers your messages. Add only the ones you will use."

Provider chooser (section 11.2, item 13): prompt "Which service should send your messages?", outlined button "Cancel". Type badges in card headers and in the switch Provider dropdown: "Twilio", "SMTP", "Telegram", "ntfy". Tiles:

Twilio: "SMS text messages, and email if you have a Twilio-authenticated domain." Creates a provider named "Twilio".
Email (SMTP): "Send from a mailbox you already have, such as Fastmail, Gmail, iCloud, or Outlook." Creates a provider named "Email".
Telegram: "Free messages through a bot you create. Best for family group chats." Creates a provider named "Telegram".
ntfy: "Free push notifications to the ntfy app. No account needed for public topics." Creates a provider named "ntfy".

Get started card (section 11.2, item 19): title "Get started". Line: "Choose how you want to send messages. You can add more providers later." Disabled Add group and Add switch hint: "Add a provider first."

Save status area (section 11.2, item 19): "Nothing to save yet" while there are no providers, groups or switches; "Configuration reset. Click Save, then restart Homebridge." immediately after a Reset.

Footer (section 11.2, item 20): the mark, then "Notify Switch v{version} · Made by Alex Rodriguez · alex-rodriguez.com · Report an issue", with "alex-rodriguez.com" linking to `https://alex-rodriguez.com/?ref=notify-switch#building` and "Report an issue" to `https://github.com/arodbuilds/homebridge-notify-switch/issues`.

Provider `name` help: "How this provider is listed when you set up a switch. For example: Twilio, Home Gmail, Family bot." Placeholder "e.g. Twilio" (or Email, or Telegram).

Advanced disclosure (section 11.2, items 14 and 28): label "Advanced", drawn as "▸ Advanced" closed and "▾ Advanced" open. ID field label "ID", button "Edit", help "How switches refer to this provider in config.json." on provider cards and "How switches refer to this group in config.json." on group cards. Credentials File help: "Optional. A JSON file, relative to the Homebridge storage directory, that holds this provider's secrets so they stay out of config.json." Link: "Where do I find this?" to `#keeping-secrets-out-of-configjson-with-credentialsfile`.

Twilio field help:

`accountSid`: "Copy from the Twilio Console home page. It starts with AC and is not a secret." Link: "Where do I find this?" to `#twilio-sms-and-email`.
`apiKeySid` and `apiKeySecret`: "Create a Standard key in the Twilio Console and paste its SID and secret. The secret is shown once." Link on the SID field: "Why not the Auth Token?" to `#api-keys`. Placeholders: Account SID "AC…", API Key SID "e.g. SK…" (from 1.3.0), Email From address "e.g. alerts@example.com", Email From name "e.g. Home", Messaging Service SID "MG…".
`smsSenders`: "Numbers you own in Twilio. Use Look up numbers to pick from your account." Info line under the list: "US numbers must be registered for A2P 10DLC or carriers will block messages." Link: "How do I register?" to `#a2p-10dlc-registration-for-us-numbers`.
`messagingServiceSid` (under Advanced): "Optional. Use a Messaging Service instead of a specific number. Found at Console > Messaging > Services. Starts with MG."
`emailFrom`: "Send email from this address through Twilio. Its domain must be verified in the Twilio Console under Email > Domains." Link: "Where do I find this?" to `#email-through-twilio`.

SMTP field help:

Mail provider picker (section 11.2, item 22): label "Mail provider", options "Fastmail", "Gmail", "iCloud", "Outlook.com", "Yahoo", "Zoho", "Other", help "Pick your mail service to fill in the server settings. Choose Other for any other mail server."
`host`, `port`, `security`: "Your mail provider's outgoing server settings. For example: smtp.fastmail.com, 465, SSL." While a preset locks them: "Filled in from the mail provider above. Click Edit to change them." with the link "Edit" on the Host label row. (The schema form has no picker, so its `host` description carries the common settings as text.)
`username`: "Usually your full email address. For example: you@example.com."
`password` with Other: "Use an app password, not your login password. Most providers require it." Link: "Where do I create one?" to `#app-passwords`. With a preset, the sentence names the provider and the link goes to its app-password page: Fastmail "Use a Fastmail app password with SMTP access, not your login password." with "Create one at Fastmail" (https://app.fastmail.com/settings/security/devices); Gmail "Use a Google app password, not your login password. 2-Step Verification must be on first; Google only offers app passwords after that." with "Create one at Google" (https://myaccount.google.com/apppasswords); iCloud "Use an iCloud app-specific password, not your Apple Account password." with "Create one at Apple" (https://account.apple.com/account/manage); Outlook.com "Use a Microsoft app password, not your login password. Two-step verification must be on first." with "Create one at Microsoft" (https://account.live.com/proofs/AppPassword); Yahoo "Use a Yahoo app password, not your login password." with "Create one at Yahoo" (https://help.yahoo.com/kb/SLN15241.html); Zoho "Use a Zoho app password, not your login password." with "Create one at Zoho" (https://accounts.zoho.com/home#security/app_passwords).
`from.address`: "The address messages come from. Your provider must allow sending from it. For example: alerts@example.com."
`from.name`: "Optional. Some providers replace this with your account's display name."

Telegram field help:

`botToken`: "BotFather sends the token. It looks like 123456789:AAF… Treat it like a password." Link: "Where do I find this?" to `#telegram`. (The schema form, which has no wizard, adds: "To create a bot, message @BotFather in Telegram and send /newbot.")
`parseMode` (under Advanced): "How Telegram reads the message. Plain text is the safest choice."

ntfy card (section 11.2, item 24):

Intro: "ntfy delivers to the ntfy app on your phone. Install the app, subscribe to a topic name of your choosing, and add that topic to a group. Anyone who knows the topic name can read it, so pick something unguessable or use an access token." Link: "Where do I find this?" to `#ntfy`.
`server`: "Leave as ntfy.sh unless you run your own server."
`auth` (label "Authentication", options "None", "Access token (recommended)", "Username and password"), help per option, with the link "Where do I find this?" to `#ntfy`: None "No credentials. Works for public topics on ntfy.sh; anyone who guesses the topic name can publish to it too." Access token "Recommended. Create an access token in the ntfy app or web app under Account, then reserve your topic so only you can publish to it." Username and password "Sign in with your ntfy username and password. An access token is safer because it can be revoked on its own."
`token` (label "Access token"): "Paste the access token. It starts with tk_ and is shown once."
`username`: "Your ntfy username." `password`: "Your ntfy password."
Group `ntfy` list (label "ntfy topics"): "Topic names as subscribed in the ntfy app. Letters, numbers, dashes and underscores." Placeholder "e.g. home-alerts-x7q2".
Switch editor, ntfy fields under Advanced (section 11.2, item 8): "ntfy title": "Optional. Defaults to the switch name." `priority` (label "Priority", options Min, Low, Default, High, Urgent): "How the app announces it. Urgent and high can break through Do Not Disturb; min shows no notification." `tags` (label "Tags"): "Optional. Up to 8, separated by commas. Emoji short codes such as warning or house show as icons in the app." Placeholder "e.g. warning, house".
Test connection lines are in section 6.5.

Telegram onboarding flow (custom UI, section 11.2, item 10):

Step 1 title: "Create your bot". Intro: "On your phone, scan this code with the camera to open BotFather in Telegram. On a computer with Telegram installed, click Open BotFather instead. Then, in the BotFather chat:" Button: "Open BotFather" (links to https://t.me/BotFather, with a QR code of the same link captioned "Scan to open BotFather"). Instructions, numbered: "Send /newbot." "Choose a display name such as Home Alerts." "Choose a username ending in bot, for example homealerts_bot." "BotFather replies with a token. Copy it and paste it below."
Connection line: "Connected to @{username}" on success; otherwise the error from the UI server.
Step 2 title: "Choose how people receive messages". Card 1: "Family group chat (recommended)": "Everyone in the group gets every message. Nobody has to opt in individually." Card 2: "Individual chats": "Each person opens the bot and taps Start once."
Step 3 with the group card selected. Title: "Add the bot to your group". QR code for https://t.me/{username}?startgroup=true captioned "Scan to add the bot to a group", the link as text, the button "Add bot to a group" (links to the same URL), the button "Copy link", and the sentence "Open Telegram on your phone and scan, or click the button, then pick your family group or create one. Everyone in the group will get alerts."
Step 3 with individual chats selected. Title: "Invite people". QR code for https://t.me/{username}?start=join captioned "Scan to start receiving alerts", the link as text, and the buttons "Enlarge", "Copy link", and "Copy invite message". The invite message copied is: "Tap this link and press Start to get alerts from our home: https://t.me/{username}?start=join"
Phone and touch alternative (section 11.2, item 16), replacing the QR code, caption and Enlarge: buttons "Open in Telegram", "Copy link", "Share" (and "Copy invite message" in the individual variant).
Before the bot is connected, step 3 shows: "Connect your bot in step 1 to get the links and QR codes."
Step 4 title and button: "Find people and groups". Help: "Lists everyone who has opened the bot and every group it has been added to. Choose a recipient group, then add people to it." Group dropdown placeholder: "Choose a recipient group…". Add without a group chosen: "Choose a recipient group first." Add with no groups configured: "Add a recipient group under Recipient Groups first." After adding: "Added to {group name}". After results are listed: "Added people appear in the group's Telegram chat IDs list. Save when you're done." No results: "No people or groups found yet. Open the bot and press Start, or add it to a group, then try again. Telegram only keeps recent updates, and a webhook set on the bot hides them."

Twilio "Look up numbers" (section 11.2, item 9):

Button: "Look up numbers". Help: "Lists the phone numbers and Messaging Services on your Twilio account so you can pick instead of typing. Manual entry always works." Dropdown labels: "Add a phone number from your account" (placeholder "Choose a phone number…") and "Use a Messaging Service from your account" (placeholder "Choose a Messaging Service…"). Status: "Found {n} phone numbers and {m} Messaging Services." More than a page: "Showing the first 20; enter others manually." No permission: "This API key cannot list numbers. Enter them manually."

Recipient Groups section:

"A group is a list of people. Switches send to groups, so you enter each person once."

Group `name`: help "Who is in this list. For example: Family, Neighbors, On-call." Placeholder "e.g. Family".
Group list labels: "Phone numbers (SMS)", "Email addresses", "Telegram chat IDs", "ntfy topics". Caption under the phone list (from 1.3.0): "Stored with the country code from Settings." Empty list lines: "No phone numbers yet.", "No email addresses yet.", "No chat IDs yet.", "No topics yet." (1.1.0 read "No email addresss yet."; fixed in 1.1.1). Add buttons: "Add phone number", "Add email address", "Add chat ID", "Add topic".
Group `telegram` list: "Use Find people and groups on your Telegram provider. IDs are numbers, not usernames."

Switches section:

"Each switch appears in the Home app. Turning it on sends your message to everyone in the groups you pick, on every channel they have, then the switch turns itself off."

Switch field help:

`name`: "Shown in the Home app. Letters, numbers, spaces, and apostrophes. For example: Water Leak Alert, Smoke Alarm." Placeholder "e.g. Water Leak Alert".
`cooldownSeconds`: "Minimum seconds between sends for this switch. 0 disables the cooldown."
`failureMode`: "Any: the sensor trips if any recipient fails. All: only if every recipient fails. Off: never trips; failures are still logged."
`failureSensor`: "Adds a sensor to this switch that HomeKit automations can watch. It opens when a message fails to send."
`failureSensorResetSeconds`: "Seconds after a failure before the sensor closes again on its own. 0 keeps it open until the next successful send."

Switch editor (section 11.2, item 8):

Enabled: checkbox "Enabled", help "A disabled switch still appears in the Home app but does nothing when turned on." Failure Sensor Reset: label "Failure Sensor Reset (seconds)" with the `failureSensorResetSeconds` help above. The Recipients, Extra recipients and Send by labels below are block sub-headings with their help as the caption under them (from 1.3.0).
Recipients: label "Recipients". Group checkbox labels "{name}: {n} SMS, {n} email, {n} Telegram, {n} ntfy" listing only the channels with entries ("Family: 3 SMS, 1 email, 2 ntfy"), or "{name}: no addresses yet". Missing group: "{id} (missing group)". Help: "Everyone in the groups you tick gets the message on every channel they have an address for." No groups: "No groups yet. Add one under Recipient Groups, or add extra recipients below." Extra recipients: label "Extra recipients", channel labels "Phone numbers (SMS)", "Email addresses", "Telegram chat IDs", "ntfy topics", empty list line "None yet.", help "People outside the groups above, entered under their channel."
Send by: label "Send by". Checkbox labels "SMS ({n} numbers)", "Email ({n} addresses)", "Telegram ({n} chats)", "ntfy ({n} topics)" ("1 number", "1 address", "1 chat", "1 topic"). Help: "Untick a channel to skip it for this switch." Nothing present: "Pick a group or add an extra recipient to choose how to send." Under a ticked channel no provider serves: "No provider configured for {channel}; add one or untick to remove." ({channel} as in the preview line).
Message: label "Message", help while SMS is ticked "Up to 160 plain characters. Emoji and special symbols are not allowed for SMS." with the placeholder "e.g. Water detected under the kitchen sink at {{time}}.", otherwise "Plain text." with "e.g. Water detected at {{time}} on {{date}}." Subject: label "Subject", help "Used as the email subject and the ntfy title. Defaults to the switch name.", placeholder "Defaults to the switch name: {name}" once the switch has a name. With Customize on, the note "Each channel has its own message under Advanced." replaces both.
Preview: "Will send SMS via Twilio to 3 numbers, email via Fastmail to 1 address, ntfy via ntfy to 2 topics." (channels in the order they are written, "SMS", "email", "Telegram", "ntfy", the provider's name, the count with the nouns above); "Nothing will be sent yet."
Advanced: disclosure label "Advanced". ID field label "ID", caption "Generated. HomeKit tracks the switch by this id, so you can rename it freely." (from 1.3.0; before, the body line "ID {id} (generated; HomeKit tracks the switch by this id, so you can rename it freely)"). Checkbox "Customize message per channel", help "Write a different message for each channel. Each starts as a copy of the shared message." Per-channel fields "SMS message", "Email subject", "Email message", "Telegram message", "ntfy title", "ntfy message" (the subject and title fields take the `subject` help below). Provider dropdowns "SMS provider", "Email provider", "Telegram provider", "ntfy provider" with the first option "Platform default ({name})", the help "For this switch only. The default for every switch is under Settings.", and "{id} (missing)" for a provider that no longer exists. `sender`: "Automatic uses the only sender, or the Messaging Service when one is set." `subject` on the per-channel fields: "Optional. Defaults to the switch name." `bcc` (email only): checkbox "Hide recipients from each other (BCC)", help "Recipients go in Bcc and your from address in To, so nobody sees the other addresses. A message to one recipient always uses To." Extra stored actions: "config.json has more than one {channel} action on this switch. The extra ones are kept as they are."

Variables toggle (next to every body and subject field, section 11.2, item 17): link-styled button "Show variables" / "Hide variables" with a chevron. Intro: "Type these anywhere in the message or subject:" List: one button per variable, `{{switchName}}`, `{{time}}`, `{{date}}` and `{{datetime}}`, each showing the token with the value it would render now beside it (the switch name, or "Switch name" while empty; "5:15 PM"; "9/8/2026"; "9/8/2026 5:15 PM" under the default settings); accessible name "Insert {{token}}". Help under the list: "Click a variable to insert it at the cursor." Link: "More about variables" to `#template-variables`.

Help toggle (card header, section 11.2, item 16): "Show help" / "Hide help".

Validation messages (settings UI): each says what the value looks like and where to get it, not the format rule.

Account SID: "That does not look like an Account SID. It starts with AC and is 34 characters; copy it from the Twilio Console."
API Key SID: "That does not look like an API Key SID. It starts with SK and is 34 characters; copy it from the Twilio Console."
Messaging Service SID: "That does not look like a Messaging Service SID. It starts with MG and is 34 characters; copy it from the Twilio Console."
Bot token: "That does not look like a bot token. BotFather sends it as numbers, a colon, then letters. Paste the whole thing."
Port: "Port is usually 465 or 587."
Switch name (and master switch name): "Use letters, numbers, spaces, and apostrophes, starting and ending with a letter or number."
Provider, group and platform name (section 5.2): "Use letters, numbers, spaces, and punctuation, up to 64 characters."
Switch editor (section 11.2, item 8): "Pick at least one group, or add an extra recipient."; "Turn on at least one channel."; "Group \"{id}\" does not exist."; "Provider \"{id}\" does not exist."; "No provider can send {channel}. Add one under Providers."; "No provider configured for {channel}; add one or untick to remove." (a ticked channel no provider serves; the same text as the note under it); "Provider \"{name}\" is {type}, which cannot send {channel}."; "Provider \"{name}\" needs an Email From address before it can send email."; "This switch reaches {n} numbers on SMS; the limit is 100 per channel." (with the channel's noun); "Message is required." and the per-channel length and character messages as before; the sender messages "Sender {sender} is not one of the provider's SMS senders.", "Provider \"{name}\" needs an SMS sender or a Messaging Service SID to send SMS." and "Choose a sender: the provider has more than one SMS sender."
ntfy server: "That does not look like a server address. It starts with https:// or http://, for example https://ntfy.sh."
ntfy topic: "Topic \"{topic}\" is not a topic name. Use letters, numbers, dashes and underscores, up to 64 characters."
ntfy tag: "Tag \"{tag}\" is not a tag. Use letters, numbers, dashes, underscores and plus signs, up to 32 characters." More than eight: "Use at most 8 tags."
Fresh cards (section 11.2, item 15): "Fill in the new {provider|group|switch} to enable Save."
Issue list (section 11.2, item 15): heading "Fix these before saving:"; collapsed past three entries to "{n} fields need attention" ("1 field needs attention") with the toggle "Show all", then "Hide". (1.1.x showed "Optional before saving:" with the default provider warning; both are gone in 1.2.0.)

Platform defaults (section 11.2, item 25): prompt "You now have {n} ways to send {channel}. Switches use {defaultName} unless told otherwise. Which should they use?", primary button "Use the selected provider", text button "Keep {defaultName}"; card header badge "Default for {channel}" and text button "Make default for {channel}"; Settings dropdown label "Default {channel} provider", placeholder "Choose a provider…" (only while nothing is written yet), help "Switches send {channel} through this provider unless a switch says otherwise under Advanced." {channel} reads SMS, email, Telegram or ntfy; {defaultName} is the provider's display name, else its id.

Time and date formats (Settings, section 11.2, item 27): labels "Time format" and "Date format"; options "12-hour (5:15 PM)", "24-hour (17:15)", "Month/Day/Year (9/8/2026)", "Day/Month/Year (8/9/2026)", "Year-Month-Day (2026-09-08)"; help "Used by {{time}}, {{date}} and {{datetime}} in messages." Validation: "Time format must be 12-hour or 24-hour." and "Date format must be Month/Day/Year, Day/Month/Year or Year-Month-Day." (reachable only from a hand-edited value, since the model falls back to the defaults).

Test send confirmation (switch card footer, section 11.2, item 11): the Test send button is replaced by "Send to {n} recipients now?" ("Send to 1 recipient now?" for one) with "Send" as the primary button and "Cancel" as a text button. Hints beside the disabled button: "Fix the errors above first", "No recipients yet". Results carry a "Dismiss" link, on the provider cards' Test connection results too (section 11.2, item 28). Remove confirmation (same item): "Remove {title}?" with the card's current name ("Remove Fastmail?", "Remove Family?", "Remove Water Leak Alert?"), or "Remove this switch?" / "Remove this provider?" / "Remove this group?" while the card has no name, with the red button "Remove" and the text button "Cancel". Red is reserved for the Remove buttons ("Remove provider", "Remove group", "Remove switch", "Remove") and the reset flow ("Reset plugin to fresh install", "Confirm") throughout the page.

Card footers: "Remove provider" and "Test connection" (provider cards); "Remove group" and "Duplicate group" (group cards); "Remove switch", "Duplicate switch" and "Test send" (switch cards). Copy names: "{name} copy", "{name} copy 2"; "Copy" for an empty name.

Unsaved draft banner (section 11.2, item 23): "You have unsaved changes from earlier. Restore them?" with the buttons "Restore" and "Discard".

Settings > Advanced (section 11.2, item 12):

Disclosure: "Advanced". Buttons, in one row: "Download backup", "Download backup without credentials". Note under the buttons: "The full backup contains your provider credentials; store it like a password. The version without credentials is safe to share when asking for help." Restore label: "Restore from backup". Restore help: "Choose a backup file. It is checked before anything changes; if it passes, the form is replaced with its contents and Save is enabled." Restore failure heading: "The backup could not be loaded:" followed by the list of problems, among them "The file is larger than 1 MB, which a Notify Switch backup never is.", "The file contains a key named \"{path}\", which is not allowed." and "The file has more than one action on the same channel on \"{name}\", which this version cannot edit. Set the switch up again instead." (several names comma separated). Restore success toast: "Backup loaded. Review the form, then click Save."; for a backup without credentials: "Backup loaded. Enter the credentials it left out, then click Save." Reset button: "Reset plugin to fresh install". Modal title: "Reset plugin to fresh install?" Modal list: "All providers, groups, switches, and settings are removed." "Switches disappear from the Home app after the next restart." "Credentials files on disk are not touched." Modal buttons: "Download backup first", "Confirm". Field label: "Type RESET to confirm." Reset toast: "The configuration has been reset. Click Save, then restart Homebridge."

Unsupported legacy configuration (section 11.2, item 26): notice "Warning: upgrading to 1.1 requires reconfiguring this plugin. Download a backup for reference, then use Reset plugin to fresh install under Advanced and set up your switches again." Switches section line: "Not shown: {names}. This switch has more than one action on the same channel, which this version cannot edit." ("These switches have" for several names, comma separated).

Using it in HomeKit (bottom of page):

"After saving, restart Homebridge. Your switches appear in the Home app. Open Automations, choose a trigger such as a sensor detecting water, and add the switch with Turn On as the action. You can also test by tapping the switch directly."

## 12. Security

1. Credentials are stored in config.json like every Homebridge plugin. The README states that Homebridge UI backups contain config.json and should be treated as containing secrets.
2. Optional `credentialsFile` on any provider: a path relative to the Homebridge storage directory (an absolute path is used as given) pointing to a JSON file whose keys override the provider's secret fields: `accountSid`, `apiKeySid` and `apiKeySecret` for Twilio; `username` and `password` for SMTP; `botToken` for Telegram; `token`, `username` and `password` for ntfy. It is read once during startup validation and again when the settings UI runs Test connection or Test send. A missing, unreadable, non-JSON, non-object, empty, or non-string-valued file is a blocking error at `providers[i].credentialsFile`; keys that are not secret fields of that provider type warn and are ignored (the key name is quoted escaped and cut to 64 characters); a notice logs which keys were taken from the file, never the values, and no message ever carries the file's text. Keeps secrets out of config.json, backups, and screenshots. Inline remains the default.
3. Twilio accepts API keys only. SMTP help steers users to app passwords. Telegram tokens are bot-scoped by nature. ntfy help recommends an access token over the account password; the token and password are redacted from every error and never logged.
4. No credential, message body (at info level), or raw provider error object is ever logged.
5. Custom UI server endpoints use submitted credentials in memory only, never persist or echo them, and only connect to the host the user configured. Every endpoint checks the shape of its payload, refuses a key named `__proto__`, `constructor` or `prototype` at any level, bounds the Test send switch id to 64 characters, and answers a plain result object; none throws.
6. TLS verification cannot be disabled.
7. Email subject and from name have CR and LF stripped. Telegram defaults to plain text.
8. Cooldown and master switch limit abuse from runaway automations.
9. Dependencies: nodemailer at runtime, plus `@homebridge/plugin-ui-utils`, which is listed as a dependency because the Homebridge UI loads `homebridge-ui/server.js` from the installed package (the plugin's main module never imports it and it has no dependencies of its own); libphonenumber-js and qrcode-generator in the UI bundle; everything else dev-only (esbuild for the bundle; bootstrap and playwright-core for the browser tests, which download nothing). `package-lock.json` is committed. Dependabot is enabled, without TypeScript major updates.
10. Publishing uses GitHub Actions with npm trusted publishing (GitHub OIDC) and provenance; no npm token is stored. Branch protection on `latest` requires a pull request.
11. "Download backup without credentials" (section 11.2, item 12) produces the file to share when asking for help: every secret field is an empty string and nothing else is removed. The README's Getting help section asks for it together with the relevant log lines.
12. Size and time bounds (from the 1.1.0 review). Outbound: 10 s timeout per attempt, at most one retry after 2 s (or the provider's retry-after, capped at 60 s), at most 5 requests in flight per provider, response bodies read up to 1 MiB; SMTP uses 10 s connection, greeting and socket timeouts. Configuration: at most 100 recipients per action after deduplication, 200 entries per address list (a group's four lists, an action's extra recipients, a provider's sms senders), 20 actions per switch, 100 providers, 100 groups and 100 switches, 8 tags per action; each is a startup error naming the field and the limit, mirrored by the custom UI. Settings UI: Find people and groups makes one `getUpdates` call for at most 100 updates and lists at most 100 distinct chats with titles cut to 64 characters; Look up numbers reads one page of 20 numbers and 20 services; a backup file over 1 MB is refused before it is read and a draft over 1 MB is neither written nor loaded; Test send accepts a switch id of at most 64 characters. Keys named `__proto__`, `constructor` or `prototype` are refused at every nesting level by startup validation, every UI server endpoint, Restore from backup and draft recovery. Every log line has its control characters escaped and every quoted configuration value is escaped and cut to 80 characters (section 8, item 6); `EMAIL_PATTERN` rejects control and format characters; the ntfy title has control characters removed before it becomes a header. Dependencies: the runtime tree has no install scripts; the dev tree carries esbuild's postinstall and the optional macOS `fsevents`, neither published.

## 13. Packaging and release

1. Based on homebridge/homebridge-plugin-template. TypeScript, ESM, ESLint.
2. `engines.node`: `^22.12.0 || ^24.0.0`. `engines.homebridge`: `^1.6.0 || ^2.0.0`.
3. `keywords` include `homebridge-plugin`, `supports-hap` and `ntfy` (from 1.1.2). `supports-hap` is required: the Homebridge verification checker requires a plugin to declare the transports it supports (homebridge/plugins README, "Declaring Supported Transports"), and this plugin publishes HAP accessories only, so `supports-matter` is not declared. `ntfy` names the fourth channel for npm search, as `sms`, `twilio`, `email`, `smtp` and `telegram` do for the others. `displayName` is "Notify Switch".
4. CI workflow: lint, build and the harness (`npm test`) on Node 22 and 24 on every pull request and push to `latest`. The browser tests need Chromium: they fail on CI without one and are skipped locally.
5. Release workflow: triggered by publishing a GitHub release. It checks out, installs, lints, builds and tests on Node 22, upgrades npm to 11.5.1 or newer (which trusted publishing requires) and fails early if it is still older, then runs `npm publish --provenance --access public` with `--tag beta` when the release is marked as a pre-release and `--tag latest` otherwise. Authentication is npm trusted publishing through GitHub's OIDC token (`id-token: write`); no token secret is referenced. `--access public` is needed for provenance on a package's first publish and is harmless afterwards.
6. CHANGELOG.md maintained per version. GitHub release notes mirror it.
7. README structure: what it does, install, five-minute Twilio SMS setup, provider setup guides with links, groups, switches, HomeKit usage, template variables, cooldown and master switch, failure sensor, `credentialsFile`, backup, restore and reset, child bridge recommendation, security notes, troubleshooting, getting help, development, changelog link, about.
8. Package contents: `files` restricts the tarball to `dist`, `homebridge-ui/public`, `homebridge-ui/server.js`, `config.schema.json` and `CHANGELOG.md` (npm adds `README.md`, `LICENSE` and `package.json`); `.npmignore` additionally excludes the source, the tests, `assets/`, `SPEC.md`, `CLAUDE.md` and `CONTRIBUTING.md`. There are no post-install scripts.
9. Brand assets live in `assets/`: `notify-switch-192.png` and `notify-switch-512.png` (icons), `notify-switch-banner.png` (2560x640, the README header), `notify-switch-social.png` (2560x1280), and `notify-switch-dark.svg`, `notify-switch-light.svg` and `notify-switch-mark.svg` (the mark alone, `currentColor`). The SVGs contain only the drawing, no metadata. The README shows the banner at full width with the alt text "Notify Switch"; the settings UI inlines the mark (section 11.2, item 20). README screenshots live there too: `switch-config.png` (a switch card, shown in the README's Switches section with the alt text "Notify Switch settings, choosing recipients and writing one message"); the harness tolerates its absence, since it is committed on its own. Nothing under `assets/` is published to npm.

## 14. Verification checklist

1. Dynamic platform. Yes.
2. Not duplicating an existing verified plugin. homebridge-messenger covers Pushover, IFTTT, and SMTP only; no verified plugin offers SMS or Telegram, multi-channel groups, or a custom UI with test sends.
3. Published to npm; source on GitHub with issues enabled; release per version.
4. Runs on Node 22 and 24; CI covers both and `engines.node` is `^22.12.0 || ^24.0.0`, matching the plugin template and Homebridge v2 guidance. Node 20 is not supported.
5. Installs and does not start unless configured.
6. No post-install scripts.
7. No TTY or non-standard startup parameters.
8. Implements the Plugin Settings GUI.
9. No analytics or tracking by the plugin itself. Twilio may add its own open and click tracking to email sent through it, and the Emails API offers no per-message switch for it (section 6.2); the README says so.
10. Files written only inside the Homebridge storage directory (only if `credentialsFile` is read; the plugin writes nothing).
11. Catches and logs its own errors; never throws unhandled.

## 15. Open questions

The questions raised for the review of this document are settled and folded into the sections above: `emailFrom` stays optional and a missing value is a validation issue on the action (section 5.2); the master switch is exposed and on by default (section 4); the failure sensor resets only on success or timeout (section 9); email bodies are plain text (section 5.5); Node 20 is not supported (section 14). What remains open for a later release:

1. Telegram `markdown` maps to the legacy `Markdown` parse mode (section 5.2). Should a `markdownv2` value be added for people who write bodies in MarkdownV2, or should `markdown` switch to it and the escaping burden move to the user?
2. Microsoft is retiring password sign-in for third-party apps on personal Outlook.com accounts (README, SMTP section). Should the Outlook.com preset stay, with its warning, or be removed once Microsoft completes the change?
3. Twilio email tracking. 1.1.1 set out to disable Twilio's open and click tracking on every send (open tracking inserts a tracking pixel, which got a message scored 5.7 as spam at Fastmail), but as of September 2026 there is no setting to turn it off, per message or per account: the Emails API documents no tracking field and the Email settings in the Twilio Console cover only event forwarding, IP addresses, and the address allow list, so nothing is sent (section 6.2). Should the endpoint gain one, the plan stands: disabled on every send, with an Advanced toggle on the Twilio provider card to re-enable it. That adds a configuration field and a UI string, so it belongs in a minor release, not a patch.
