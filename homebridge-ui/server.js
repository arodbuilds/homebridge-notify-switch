/**
 * Entry point the Homebridge UI starts for this plugin's custom settings screen.
 * The implementation is compiled from src/ui/server.ts into dist/ui/server.js.
 */
import { startUiServer } from '../dist/ui/server.js';

startUiServer();
